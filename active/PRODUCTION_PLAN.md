# Production plan

This plan begins intentionally uncompleted. Checkmarks require retained evidence; completed detail moves to `archive/HISTORY.md`.

## Public issue and Reddit intake (2026-09-23)

- [x] Package and run corrected F: install with the known five ZIPs and selected Private firewall access: `0.6.45.9012` completed and exact-verified all three games and both packs. Effective rules matched all three F: executable paths with Private allow/Public block; first launches reached Vengeance and Black Knight pilot creation and Mercenaries roster without a Windows Security prompt. The cursor was not at bottom-right in observed Vengeance/Black Knight screens. This does not complete the separate mission/input matrix, private-declined rule behavior, or uninstall rule cleanup.
- [ ] [#8](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/8), input qualification from [the source survey](INPUT_DISPLAY_RESEARCH.md): on public `0.6.45.9012`, Mercenaries on Windows 10/RX 6750 XT/1440p reportedly snaps the torso down/right and ignores movement when mouse torso control is enabled in Instant Action; inverted mouse control instead snaps it upward and locks. Keyboard numpad torso control reportedly works. Reproduce this exact mode separately from the earlier bottom-right *initial cursor position*, high-FPS/dead-zone behavior, and joystick enumeration. Record Windows scaling, game-process DPI awareness, physical cursor/clip rectangle, client rectangle and center, mouse-control bindings, and the installed profile; A/B the launcher lifecycle guard and dgVoodoo mouse capture in disposable copies before choosing a game-local reversible fix. Verify mouse torso and menu controls, keyboard/mouse rebinding in menu and mission, persistence across relaunch, and two consecutive missions in all three titles. The current 30 FPS cap and dgVoodoo cursor-only hook already implement documented upstream remedies; `/gosNoJoystick` currently disables native stick input.
- [ ] HOTAS qualification from [the source survey](INPUT_DISPLAY_RESEARCH.md): exact installed binaries for all three titles contain dynamic `dinput.dll`/`DirectInputCreate(Ex)` references rather than static DirectInput imports, confirming where to trace next. Instrument actual calls and visible device GUIDs, then A/B one stick, separate stick/throttle/pedals, and one externally merged virtual stick. Evaluate the already pinned zlib `dinputto8` candidate without `/gosNoJoystick`, including its reported second-mission and in-mission-binding crashes. `devreorder` can select/order DirectInput 8 devices but does not combine them; Joystick Gremlin plus vJoy documents the existing multi-device merge route. Do not package an input DLL or driver until the complete three-title matrix and provenance gates pass.
- [x] Prepare an emulated-HOTAS test path in response to unavailable physical controls: inventory found no virtual joystick service or present game-controller device; the upstream Brunner vJoy 2.2.2.0 installer was downloaded to ignored research space, Authenticode-verified, SHA-256 recorded, and Defender-scanned clean. It remains uninstalled pending the controlled driver-state test. A virtual stick can cover enumeration/binding/crash behavior but cannot close physical-hardware coverage.
- [ ] Optional gameplay-only widescreen from [the source survey](INPUT_DISPLAY_RESEARCH.md): preserve 4:3 as default and preserve menus, prerendered movies, opening/closing and pre-mission cinematics, and any authored in-engine scene at 4:3. During active play only, extend the **world view horizontally** into the pillar areas with correct FOV/culling and unchanged central HUD/cockpit and cursor mapping. First prove a reliable play-versus-cinema signal for all three exact retail executables; all contain `CinemaState` and `cinemastart`/`cinemaend` strings, but the current dgVoodoo observer cannot see that state. Then prove 16:9 and 16:10 matched-frame behavior and scene transitions before exposing a reversible launcher checkbox. A post-render crop/stretch or raw widescreen resolution fails this contract. WSGF's MekTek MP3.1 result has stretched HUD and Hor+ real-time cutscenes, so it is feasibility evidence only.
- [ ] Reopen gameplay seam correction for edge-content loss. The former five-pixel top/left crop operated on an 800x600 source and cost about 12 displayed pixels per affected edge at 1920x1440; the `0.6.46` candidate reduces this to two source pixels. The reported bottom loss needs separate measurement. Capture identical paused frames at crop 0/1/2/5 and stock wrapper, measure all four edges and the seam, then choose the smallest content-preserving source correction. Preserve 2D screens and prove all three titles at more than one output size before marking the presentation result qualified.
- [x] Prepare bounded seam A/B builds without changing the packaged default: the dgVoodoo add-on builder accepts explicit experimental source crop values 0/1/2/5; all variants compiled, and the default 5-pixel build reproduced the pinned patch and DLL SHA-256 exactly. The visual and edge-measurement gate above remains open because Windows capture returned GPU-device-suspended `0x887A0005` during this session.
- [x] Remove redundant launcher status hashing: one `InstallStatusReader.Read()` now verifies each physical game tree once, sharing the Vengeance result across Black Knight and both packs. Core smoke and launcher build pass; the F: test tree reported all three games Ready in 3.14 seconds. Recheck packaged UI responsiveness when native capture works.
- [x] Current-source packaged CUE/F: regression: isolated internal `0.6.45.9008` installed from four supplied ZIPs plus a CUE/MODE1/2352 BIN regenerated from the supplied Inner Sphere ISO, all staged on F:. Vengeance, Black Knight, Mercenaries, and both packs exact-verified under `F:\Mechwarrior Test\app-cue`; the worker ended `All selected games installed and verified.` All three game executables opened titled windows using production launcher arguments. Freshly backed-up C: HKCU registrations were temporarily redirected to F: for this isolated test, then restored and verified; the watchdog was disarmed. Evidence: ignored `.local/f-cue-packaged-success-worker-20260923.log`. This checks the packaged CUE intake path with known source bytes; actual alternate BIN revisions, full gameplay, clean-user setup, and uninstall remain open.
Reports from [GitHub issues](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues) and the [release discussion](https://www.reddit.com/r/mechwarrior/comments/1wkat18/mechwarrior_4_remaster/) are field evidence, not proof of a common cause. Preserve the exact affected media revision, Windows/GPU, installed profile, and sanitized worker/game log in each reproduction. The five ZIPs supplied at `C:\Users\icehe\Documents\Mechwarrior 4 ISOs` are byte-identical to the five existing ignored ZIP fixtures, so they provide a fresh real-media regression but no new edition coverage. An ISO container hash alone is not a suitable universal acceptance rule: the media catalog identifies disc structure, while exact hashes remain necessary at executable, official-patch, and transform boundaries. Near-match media intake now identifies up to two missing expected files when one layout is uniquely closest, but still rejects the unknown edition. Add a new edition only after its file inventory and transform outputs are qualified; never bypass those gates for an unknown image. [Redump](https://redump.info/discs?advanced=1&dumper=RexPearce&letter=M&region=&status=&system=) lists alternate original retail revisions and [BetaArchive](https://www.betaarchive.com/database/view_release.php?uuid=a53085b9-88d9-47c1-904a-19f86786b136) catalogs a 2004 compilation; these are candidate metadata, not qualified media. Redump provides hashes rather than disc bytes, and the third-party full-disc download listings found in this search do not establish redistribution permission. No alternate ISO was downloaded.

- [ ] Candidate CUE/BIN revisions supplied 2026-09-23: 29 MechWarrior 4 CUE sheets describe 16 Vengeance, 3 Black Knight, 7 Mercenaries, and 3 Mech Pak candidate pressings; 2 more sheets are out-of-scope MechWarrior 2. All MW4 sheets name one MODE1/2352 BIN track at index zero, but no companion BIN is present. Source now accepts this bounded CUE/BIN format: it resolves only a same-directory BIN, validates raw sectors, projects Mech Pak resources directly without materializing legacy setup/DRM, and converts base-game data into an owned temporary ISO for existing structural recognition. Synthetic recognition, byte-round-trip, rejection, and cleanup tests pass; a locally supplied real Inner Sphere ISO round-tripped through synthetic raw BIN and was recognized. The wizard accepts CUE input, and the current-source packaged F: install passed with a CUE/BIN generated from the known Inner Sphere ISO; no alternate-revision install was qualified. The media catalog already accepts non-identical ISO containers with the same required layout; executable and official-patch transforms still demand qualified game-file inputs and exact outputs. The CUE filename cannot establish those bytes. When a user supplies an actual different BIN or extracted disc tree, fingerprint only the necessary files, test whether the official update converges to known outputs, then add a narrow revision contract if required. Keep raw images/CUEs untracked and never relax executable checks solely because a CUE title resembles a known edition.

- [ ] [#5](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/5): Patch 3 reportedly produces an additional `MW4 Dedicated Server.bat` on original-disc media. Source now permits only the exact 25-byte, SHA-256-pinned shortcut known from local official-update output in contained scratch and excludes it from the installed payload. A fresh transform against the available retail Disc 1 plus Inner Sphere Patch 3 media passed and all 11 retained payload paths/hashes matched the prior qualified reference; the server shortcut remained absent from output. Mismatched patch inputs/outputs now report size and SHA-256 for variant qualification. Confirm the reporter's extra-file hash, then run that original-disc combination through full Patch 3, Black Knight PR1, exact install verification, and uninstall before closing.
- [ ] [#7](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/7), [#2 follow-up](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/2): the original `0.6.44` report measured a 5126x2189 window on a 5120x2160/RTX 5090 monitor. Public `0.6.45.9012` now pins the **outer** window to the physical monitor rectangle, but the reporter's new gameplay screenshot still shows clipped left/right HUD content, with the remaining crop apparently bottom-anchored. Therefore the outer-window correction is insufficient and #7 stays open. On the affected system, capture outer and **client** rectangles, DWM extended frame bounds, actual swapchain/backbuffer and final 4:3 image bounds, monitor scaling, and a menu-versus-gameplay matched frame. A/B the lifecycle guard, dgVoodoo borderless/fullscreen attributes, and the source-built five-pixel gameplay seam crop in disposable copies; compare stock windowed 4:3 against borderless. Correct the owner of the demonstrated crop without stretching or moving 2D assets, then verify complete menus/gameplay, centered pillarboxes, cinematics, Alt-Tab, and cursor release at 5120x2160 and 3440x1440. Do not mark fixed from outer-window geometry or a non-ultrawide monitor alone.
- [ ] [#2](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/2), [#4](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/4): distinguish empty Windows `Get-DiskImage` query output, pre-attached images, mount cleanup, and media layout failures. Source requires exactly one Windows disk-image record, reports an empty attachment state clearly, and tells users to eject an externally mounted ISO before retrying. A fresh Vengeance ZIP probe under a non-admin token reproduced `Get-DiskImage` access denial, which formerly emitted raw CLIXML and temporary paths; the backend now classifies access denial and suppresses raw PowerShell stderr with focused tests. The Mech Pak ZIPs were recognized without mounting. Reproduce on Windows 10 with the affected drive/storage arrangement and capture sanitized command category plus attachment state. Preserve the owned-mount rule. The #2 reporter later booted after replacing ISOs, so a single ISO-version explanation is unproven.
- [ ] [#6](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/6): setup now explains that Black Knight needs both Vengeance discs, its own disc, and a Mech Pak disc for official PR1. The supplied log shows an incomplete selected media set from Black Knight alone; this does not establish a Black Knight transform failure. Verify the message in the packaged wizard and keep the original expansion topology.
- [ ] [#1](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/1): reproduce Vengeance `DirectDrawCreateEx(0x80004005)` and the later null-call crash on Windows 11/RTX 5090. Mercenaries works on that system. A source-only x86 `DirectDrawProbe` checks `DirectDrawEnumerateA` and `DirectDrawCreateEx(NULL, IID_IDirectDraw7)` with either Windows' DLL or an exact game-local dgVoodoo DLL, reporting hashes and HRESULTs. An optional windowed stage also calls `SetCooperativeLevel(DDSCL_NORMAL)` and creates a primary surface. On this non-5090 PC, all four calls returned `0x00000000` and object pointers for Windows and all three F: game-local wrappers; each wrapper/profile/D3DImm hash matched the pinned package. Isolated D3D12, D3D11, and D3D11 WARP profiles likewise passed. A deliberately invalid OutputAPI profile also passed all four calls, proving the probe still does not validate backend initialization or game rendering. It narrows an early DLL-load/API/surface failure, but cannot emulate the 5090 driver or prove the affected path. Run it against the reporter's exact installed wrapper and system DLL, compare their sanitized game log and driver/monitor details, then test any graphics-backend change across all three titles and movies. Do not attribute this to an ISO hash or switch backends globally without evidence.
- [ ] [#3](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/3) and Reddit graphics feedback: the Settings dialog now opens immediately as a single instance and offers render resolution, resampling, anisotropic filtering, and antialiasing for all installed titles. It distinguishes render size from the monitor-sized 4:3 display surface. The isolated F: .9011 install exposed game crashes at 1200x900 and 1440x1080, so those and untested higher render modes have been withdrawn from source; see the field correction below. Package the corrected source, then field-check the remaining choices through menus, cinematics, gameplay, Alt-Tab, repair, and uninstall before shipping. Gameplay-only widescreen has a separate gate above.
- [ ] Correct the superseded preset claim before release: the F: `.9011` packaged test proved that `1200x900` and `1440x1080` crash at Basic Training with `HUDNumberText::GetIndex`, and 1200x900 mis-maps the menu pointer. Exact executable disassembly for all three titles then found the HUD dispatch accepts only heights 480, 600, 768, 1024, and 1200. Current source therefore lists only 800x600, 1024x768, and 1600x1200, chooses a listed mode for Automatic, and still displays the monitor's full 4:3 area through upscaling. A scratch-only Vengeance branch probe entered Training at a requested 1200x900, but the game rewrote its INI height to 600; that does not qualify the native mode. Package the corrected source and verify 1600x1200 menus, pointer, cinematics, and gameplay in all three titles. The user's 900p, 1080p, and 4K requests remain open pending a proven engine/HUD fix. Do not ship `.9011`.
- [ ] Reproduce the reported bottom-right mouse start under a packaged launcher at 800x600, 1024x768, and 1600x1200, with Windows scaling recorded. Capture physical cursor coordinates, client coordinates, and the game-window bounds before and after focus and Alt-Tab. Separate a one-time initial placement error from continuous down/right drift and the unsupported-resolution menu mapping error. Fix at the owner of the observed coordinate transform, then verify menus and mission input in all three titles.
- [ ] Reddit input/coverage: reproduce downward/rightward mouse drift and controller/joystick non-detection in all titles before adopting `dinputto8`; capture two missions and rebinding per title. Record Black Knight campaign MechLab, original-language discs, and LAN/multiplayer as separate qualification matrices. Linux/Proton remains outside the current Windows release contract; optional true widescreen gameplay is newly scoped above but remains unqualified and off by default.
- [ ] Non-C: destination report: an isolated test-only `0.6.45.9006` package installed from all five hash-verified original ZIPs on NTFS `F:` into `F:\Mechwarrior Test\app`. Vengeance plus Black Knight with both Mech Paks and Mercenaries completed official transforms, committed, and exact-verified; graphics, registration, and presentation checks ended in worker success. All three F: game executables opened titled windows with production launcher arguments. The three live C: HKCU game registrations were temporarily redirected using exact .reg backups and a 15-minute restore watchdog, then reimported and verified against existing C: executables; the watchdog was disarmed. The F: test app and media remain for inspection, with worker evidence in ignored `.local/f-clean-success-worker-20260923.log`. Earlier runs proved that a second install with live C: registration is rejected before game-tree mutation; source now gives that conflict a specific worker exit code and actionable wizard text. This local F: success does not reproduce the reporters' failing machines, full gameplay, clean-user setup, or a separate ISO revision. Continue with reporter logs covering destination filesystem/permissions, security tooling, media revision, and exact stage; run packaged UI/settings/diagnostics and ownership-safe uninstall when isolated test registration is available. A stale uninstall entry from an older isolated `A1E6047D` test package still points at a deleted F: shell; medium-integrity removal was denied and must never touch the production AppId.

## Issue #7 and #8 resolution slices (2026-09-24)

These are separate defects. Work through the bounded steps below before changing release defaults. Existing `GameResolution.LargestFourByThree(5120, 2160)` already yields `2880x2160`, while the actual selectable MW4 render mode is `1600x1200`; changing the displayed resolution label or permitting unqualified 2160-high game modes cannot resolve #7.

The `0.6.46` release candidate changes two independently identified owners. The window guard now fits the **client area** to the physical monitor, preserving an oversized outer frame when needed; synthetic framed and borderless Win32 smokes pass. The cursor guard yields to an existing game/wrapper clip and releases only its own clip; synthetic foreground/disposal smokes pass. The source-built gameplay seam trim is reduced from five to two 800x600 source pixels, based on the retained 2–3 displayed-pixel seam measurement; a matched game-frame A/B is still required. The Settings ultrawide presets are previews for 2560x1080, 3440x1440, and 5120x2160, with centered 1440x1080, 1920x1440, and 2880x2160 4:3 areas respectively. They do not change Windows display mode or request unsupported MW4 render heights. The locally built setup passed release-tree policy and a current Defender scan. Affected-ultrawide and affected-mouse field confirmation remains open even if local regression passes.

### #7: ultrawide borderless crop

1. [x] Establish the new field baseline: the reporter's `0.6.45.9012` gameplay capture is reported to still clip HUD content after the outer-window pin, whereas the original `0.6.44` report measured a 5126x2189 outer window on a 5120x2160 monitor. Pixel inspection of the *cropped* new PNG found an exactly 2880-pixel-wide active image in its middle row, matching the expected 4:3 width at 2160 high; the PNG cannot establish full-monitor centering. A retained local 2560x1440 gameplay capture also places the stock HUD at the 4:3 edges. The remaining visual complaint is real field feedback, but neither the image width nor the screenshot alone identifies the layer or amount of new content loss.
2. [x] Extend the source-only window lifecycle probe to record the client rectangle in screen coordinates, DWM extended frame bounds, DPI awareness, window DPI, and cursor/clip coordinates along with the existing outer window rectangle. It compiles; it has not run on the reporter's hardware. The former outer-window pin shrank a framed window's client area below the monitor size. The production guard now fits the client rectangle to the 2560x1440 monitor in two desktop component smokes, one borderless and one with the reporter's +6/+29 outer oversize, while preserving a tighter synthetic wrapper cursor clip. This proves Win32 geometry, not dgVoodoo's 5120x2160 final image.
3. [ ] On the affected 5120x2160 monitor, compare the full desktop and exact game-window captures in menu, cinematic, and paused gameplay with the launcher guard active. Record the new geometry fields plus actual 4:3 image boundaries. Repeat with the guard disabled and with dgVoodoo `WindowedAttributes`/`ScalingMode` alternatives in disposable game-local profiles; preserve the original installed profile. In particular, test dgVoodoo's documented `FullScreenMode=true` plus `FullscreenAttributes=fake` as an alternative to the current forced-windowed `borderless, fullscreensize` path; that changes window/capture semantics and requires movie, Alt-Tab, and input regression before adoption.
4. [ ] Compare the source-built gameplay-only seam crop at 0, 1, 2, and 5 source pixels on matched gameplay frames. If the five-pixel correction clips HUD, reduce it only as far as seam removal requires. If the client rectangle or wrapper scaling is responsible, correct that layer instead. Do not alter 2D menu/movie geometry to fix a gameplay-only defect.
5. [ ] Verify the fix at 5120x2160 and 3440x1440 with complete 4:3 menus, cinematics, cockpit/HUD, centered pillarboxes, Alt-Tab, and pointer mapping; run the same all-title regression at 2560x1440 before packaging. A geometry-only result without a visual comparison does not close #7.

### #8: mouse torso lock

1. [x] Establish the field baseline and upstream bounds: `0.6.45.9012` Mercenaries on Windows 10/RX 6750 XT/1440p snaps the torso down/right and ignores mouse movement in Instant Action; inversion snaps it upward and numpad control reportedly works. The project already uses dgVoodoo's MW4 cursor-only hook and a 30 FPS cap. A longstanding MW4 field report attributes similar corner pull to Windows display scaling, but that is a hypothesis for this build.
2. [x] Remove a proven launcher-versus-wrapper cursor ownership conflict in source: the lifecycle guard previously reapplied `ClipCursor` to the whole monitor every 25 ms while foreground and cleared the desktop-wide clip unconditionally during disposal. It now leaves an existing dgVoodoo/game clip alone, claims an unrestricted cursor only once per activation on a multi-monitor desktop, and releases only its own unchanged clip. The source-built `CursorClipGuardSmoke` uses the real guard on a foreground synthetic game window; it verified the wrapper's tighter clip persists during activity and disposal. Core smoke, launcher build, presentation/user-flow/packaging/release-tree contracts pass. This is a candidate #8 improvement, not a mission-level fix claim.
3. [x] Build a source-only, read-only `InputCoordinateProbe` that attaches to exactly one running MW4 title and samples the exact foreground/client/monitor/cursor-clip/DPI state for 15 seconds without altering the game. Compilation and the no-game rejection path pass. A desktop synthetic process named `MW4Mercs.exe` produced 92 bounded snapshots over a ten-second request, with the expected 2560x1440 monitor, 96 DPI, and foreground state; a real mission sample remains open.
   The first real Mercenaries baseline on the existing registered C: installation captured 92 snapshots at the roster: outer/client/monitor 2560x1440, cursor clip 0,0–2560,1440, window DPI 96, DPI awareness 0, and cursor 799,599. A later 30-second read-only trace during interactive use captured 274 foreground samples; the cursor ranged x=46–868/y=232–616 and returned to 799,599, with the monitor clip in 271 samples and the 6400x1440 virtual-desktop clip in three. During the user's own Mercenaries test, a further 274 foreground samples ranged x=600–841/y=575–612, returned to 799,599, and had 272 monitor-wide plus two virtual-desktop clip samples. The user reported no down/right drift on this machine. This is a local baseline, not torso-lock reproduction or evidence of the reporter's DPI/GPU behavior. User input was detected by computer control, so automated game interaction stopped. The probe now gives each run a timestamped filename to retain comparisons.
   A source-built `0.6.45.9013` Mercenaries-only F: smoke setup passed Defender, but its worker refused to overwrite the still-live C: Mercenaries registry entry. It left only the uniquely identified Inno setup shell at `F:\Mechwarrior Test\issue8-guard-9013`, with no F: game. That shell was removed with its own uninstaller; the C: registration and game installation remained intact. The new guard still needs an actual mission after the normal C: reinstall replaces the installed launcher.
4. [ ] Reproduce the exact torso-axis setup at 100%, 125%, and 150% Windows display scaling in a disposable, launcher-started Mercenaries tree. Capture options/bindings before and after the change, whether menu pointer movement works, and window/client/clip/cursor/DPI snapshots immediately before mission load and during the lock. Check whether the game resets or moves the OS cursor each frame. Preserve original input configuration for comparison.
5. [ ] A/B one variable at a time: the previous versus ownership-corrected launcher cursor guard; dgVoodoo `CaptureMouse` and `FreeMouse`; application-controlled versus wrapper scaling; and a reversible, game-local Windows high-DPI override. Compare physical and game-coordinate centers. Do not apply a system-wide scaling change as the product fix or enable broad GDI hooking, which dgVoodoo's author found flickers in MW4.
6. [ ] Implement only the measured owner fix, then verify torso and aim axes, menu mouse, keyboard fallback, focus loss/return, binding persistence, and two consecutive missions in all three retail titles at representative DPI values. Keep joystick/HOTAS qualification separate.

## Immediate polish priorities

The `0.6.22` field run established the first all-three-games-playable baseline. Work now proceeds in this order, without regressing original cinematics, 4:3 artwork, or media-only installation:

0. [x] Remove the Defender-triggering whole-disc Mech Pak ZIP path. Setup now reads supported pack ISOs in memory, writes only exact allowlisted resources and official patch inputs, and never materializes or mounts their legacy setup/DRM content. Real Inner Sphere projection, a complete five-ZIP install, and Defender scans of projected and installed trees pass. Reproducible setup packaging remains the release gate.

1. [x] Proportionally zoom only Vengeance's live-action opening movie until its pillar bars are gone and its letterbox bars are as thin as the target monitor permits. The source-built dgVoodoo add-on requires both exact process `MW4.exe` and an exclusive-open conflict on `Content/Movies/GAMEOPEN.MPG`, derives the movie's 30:17 source geometry, and center-covers to the active monitor without stretching or rewriting media. Exact-window captures prove the preceding startup clip and following menu remain strict 4:3. Black Knight, Mercenaries, all other videos, menus, and gameplay bypass this gate.
2. [x] Re-crop the Vengeance manual to the cover artwork's true first-page bounds, apply that page size consistently to the remaining pages, regenerate the launcher cover, update the deterministic lock, and render every page for clipping/order review.
3. [ ] Ship a maximum-quality, monitor-adaptive renderer profile for all three titles. Source detects the physical pixel dimensions of the monitor containing the foreground launcher, derives the largest fitting 4:3 render surface, and uses the same captured result for the launch request and guarded INI; `1920x1440` is only the result for a `2560x1440` monitor. Setup `0.6.40` carries the `0.6.39` upgrade of pre-existing low-detail pages to MW4's Ultra High-equivalent game-owned settings without preventing later user changes. The profile preserves desktop mode, strict 4:3 geometry, 4x MSAA, 16x anisotropic filtering, and 32-bit color. Automated geometry, low-to-ultra upgrade, core smoke, focused contracts, reproducible packaging, and Defender gates pass; moved-launcher multi-monitor and packaged all-title gameplay proof remain before completion.
4. [x] Remove the field-proven green seam only during gameplay and in-engine 3D. Retained runtime logs proved the shell and scene both present from `800x600`, invalidating the former high-resolution classifier. The shipped correction gates a five-source-pixel top/left crop on dgVoodoo's active legacy Direct3D-device lifecycle and anchors the image bottom-right; menus and startup videos remain on the untouched 2D path. The `0.6.36` packaged-launcher mission test field-confirmed that the top/left seam is gone without regressing 4:3 presentation.
5. [ ] Convert installed Inner Sphere and Clan resource evidence into game-visible Mech Pak activation. `0.6.37` proved the executable correction installed exactly but stock Cauldron-Born still failed because its loadout references separately flagged component records. Exact archive inspection found four Clan SMRMs and Inner Sphere Heavy Gauss in `weaponstable.mpt`, plus Clan Enhanced Optics and Inner Sphere IFF Jammer in `subsystemtable.mpt`. `0.6.38` unlocks selected-media records across those tables plus both chassis tables and includes an exact-hash owned archive migration for existing installs; the user field-confirmed stock Cauldron-Born selection no longer raises the entitlement dialog. Generated Vengeance and Black Knight archives contain zero remaining selected-pack flags. Broaden coverage to both families, all three titles, all eight chassis, campaign, and multiplayer without altering campaign markets.
6. [ ] Match the required capture-friendly Alt-Tab behavior in all three titles. On focus loss, the borderless game picture must remain visible, unchanged, and non-minimized behind other windows; screenshots and video capture must retain the same geometry. Alt-Tab or a single click must return cleanly, with the cursor released while inactive and recaptured exactly once when active. Current source finds the exact PID's largest visible legacy window, restores it without activation, pins its stabilized rectangle, and clips only while foreground. A disposable-launcher startup-presentation probe passed every state/geometry/display/cursor check on the current system, using exact-window capture only. Keep this item open until the same matrix passes during gameplay in all three titles.
7. [x] Complete end-user attribution. Package a readable credits/third-party notice covering incorporated upstream/runtime and build-time work, exact versions/revisions where pinned, author/project, license/provenance links, and local use/modification; expose it from the installed folder and add a compact launcher Credits action beside Uninstall.

## 1. Evidence and legal boundary

Outcome: supported inputs and publishable outputs are explicit and defensible.

- [x] Hash and structurally inventory each local ISO/archive/manual without tracking proprietary data.
- [ ] Distinguish original retail, official patches, Microsoft-released Mercenaries freeware material, community releases, and third-party cracks by source and license.
- [x] Define the release payload allowlist and forbidden-file scanner.
- [x] Record a media/redistribution ADR.

## 2. Reproducible installation core

Outcome: user media becomes a verified, rollback-safe installation without invoking unsupported legacy setup paths where avoidable.

- [x] Detect supported Vengeance media layouts and stage a disposable install.
- [x] Implement the proven official update chain: Vengeance Patch 3, Black Knight PR1 in the shared `MW4X` topology, and Mercenaries PR1 before any disc-check transform.
- [x] Reimplement and independently vector-test the SafeDisc 1 eight-byte block cipher, title-specific 1.50.20 page-local second layers, and exact-input Vengeance/Mercenaries PE/import/pointer repair transforms.
- [x] Extend the shared transaction contract to Black Knight and Mercenaries, including contained cabinet extraction for Mercenaries.
- [x] Diagnose the Inner Sphere and Clan 64-bit failure boundary from installer/payload evidence: plain resource payload plus official game patch plus obsolete SafeCast/C-Dilla entitlement; replacement remains unqualified.
- [x] Install optional packs from exact-hash media through the Patch 3 and resource-overlay path without invoking C-Dilla/SafeCast; in-game chassis/variant visibility remains a separate qualification gate.
- [x] Persist an ownership manifest sufficient for repair and safe file uninstall.
- [x] Add atomic multi-source readiness for all games and packs, and enable dependency-ordered mutation for all three qualified game paths.
- [x] Reopen and re-recognize selected directory/ISO/ZIP sources under one disposable transaction lifetime with success and failure cleanup.
- [x] Add read-only destination planning with contained per-game folders, conservative space budgets, current-volume capacity, and unsafe-root rejection.
- [x] Add cooperative cancellation for installer media inspection/revalidation with owned mount and ZIP scratch cleanup.
- [x] Encode the original product topology: Vengeance base, Black Knight dependent expansion, independent Mercenaries, and Vengeance-dependent Mech Paks.
- [x] Apply that topology to destination planning and remove the obsolete hidden Black Knight-only installer execution path.
- [x] Collect a neutral multi-file ISO/ZIP set in the package wizard before Install and hand it directly to intake validation without a second picker. Source now also exposes CUE selection; packaged CUE use remains open above.
- [x] Remove the second visible WinForms installer; the package wizard now owns the entire visible flow and invokes only a synchronous hidden install worker.
- [x] Remove the caller-supplied Vengeance replacement executable from installation requests; confine the qualified retail media-derived transform to coordinator-owned scratch and fail closed on unknown revisions or output hashes.
- [x] Remove the caller-supplied Mercenaries replacement executable; derive its disc-free executable from exact original-media inputs inside coordinator-owned scratch and fail closed on unknown revisions or output hashes.
- [x] Prove official Vengeance Patch 3 applies directly to retail media, apply it through a constrained source-owned host, retain only exact qualified outputs, and remove the obsolete C-Dilla and ARTP imports/call sites from the generated executable.
- [x] Recognize the exact official Mercenaries PR1 updater from the supported fix ZIP, exclude its adjacent historical no-CD payload, apply PR1 in contained scratch, and derive an exact-hash source-owned `50.07.01.2105` executable before commit.
- [x] Make original Mercenaries media sufficient for the internal AIO flow by build-time extracting and hash-locking only the two qualified official PR1 payload files; public redistribution rights remain an explicit release gate.
- [x] Remove the failed app-local PR1 runtime and field-disproven static v2/v3 outputs. Apply official `45.30.04.1908`, capture its decrypted user-media image only during setup with the exact source-built GPL DLL, reproduce the established PR1 import layout, exact-correct 110 reviewed operands, repair all 127 tail branches, reject residual removed-tail references, and emit deterministic static v4 `b31bd051…`. Reject retail until its separate Vengeance Patch 2 prerequisite is implemented.
- [x] Apply official Black Knight PR1 and Mercenaries PR1 in contained reference trees and retain exact pre/post manifests as ignored evidence.
- [x] Replace separate Vengeance/Black Knight destinations with one atomic schema-2 Vengeance-family manifest and preserve the original `MW4X` subdirectory.
- [x] Remove the superseded retail Black Knight proxy DLL from production package staging and constrain the RTP host to the three qualified official-update hashes.

## 3. Launcher and user experience

Outcome: one MW4-styled launcher makes installed capabilities obvious and starts each title reliably.

- [x] Choose the application stack and record an ADR.
- [x] Create a compact MW4 visual direction using the user-approved icon and the proven MW3 launcher hierarchy without adding unlicensed web art.
- [x] Show Vengeance, Black Knight, and Mercenaries launch actions only for manifest-verified installations; distinguish repair-required trees.
- [x] Show Inner Sphere and Clan pack status from exact installed-file evidence with clear installed/missing states.
- [ ] Complete settings, diagnostics, and repair actions. SETTINGS opens one loading dialog immediately and offers Automatic fitting among listed game render modes, explicit 4:3 modes through 1920x1440, scaling filter, 1–16× texture filtering, and off/2×/4×/8× antialiasing globally. The 1200x900 and 1440x1080 render modes are withdrawn after packaged F: mission crashes; modes above 1920x1440 remain untested. The display area still follows the monitor at 4:3, with truthful scaling labels. Core smoke and source contracts pass; package and game-test the correction before release. DIAGNOSTICS copies a read-only, path-free report. Repair still needs an ownership-safe setup path.
- [x] Consolidate the supported games' first-run EULA state into one explicit setup acceptance and exact transforms/registration so gameplay is not interrupted by legacy dialogs.
- [x] Expose one launcher uninstall action that removes verified owned game payloads, closes the launcher, and visibly hands off removal of the application shell, manuals, shortcuts, and registration.
- [x] Keep shell uninstall available when an old or damaged game tree lacks verifiable ownership, preserve uncertain files, and remove the shared Vengeance/Black Knight physical tree only once.
- [x] Create and wire the requested Vengeance remaster icon with the MW3 launcher's silver/gray lower-right `R`; retain original-art licensing as a release gate.
- [x] Replace generic letter/manual markers with deterministic cover thumbnails generated from and packaged beside each cleaned manual.
- [x] Reproduce and fix the field-confirmed incorrect-install failures; candidate `0.6.7` installed and reached the real menu in all three titles.
- [x] Field-qualify installation, launch, pilot creation, and playable game entry for Vengeance, Black Knight, and Mercenaries on the current NVIDIA/Windows test system using setup `0.6.22`.
- [ ] Qualify the polished presentation profile: native-monitor 3D scaling, maximum safe in-game detail, 4x MSAA, 16x anisotropic filtering, 32-bit color, Vengeance-only widescreen intro framing, non-minimizing Alt-Tab, and clean return in all three titles. Post-UAC topmost Setup behavior remains a separate user-visible gate.

## 4. Manuals

Outcome: readable manuals are generated reproducibly from user-local scans and packaged only when redistribution rights permit.

- [x] Inspect page geometry and render every raw manual.
- [x] Split Black Knight's combined cover, move the back cover to the end, and verify page order.
- [x] Re-crop Vengeance to the cover's exact visible page bounds and apply the resulting 509.8 x 323-point geometry consistently across all 98 pages; regenerate the cover, exact-lock the new PDF, and visually inspect every rendered page. The earlier 342-point heuristic was field-rejected for retaining a bottom white strip.
- [x] Confirm Mercenaries needs no geometry cleanup.
- [x] Add page-count, dimensions, content-presence, deterministic-output, and visual render checks.
- [x] Generate, exact-hash, package, and visually verify one launcher cover thumbnail per manual.

## 5. Compatibility qualification

Outcome: all installed titles and optional packs work on supported modern Windows configurations.

- [ ] Establish clean baselines for video, audio, input, movies, configuration, saves, and multiplayer behavior.
- [x] Retire the rejected DDrawCompat presentation branch after dgVoodoo2 superseded it; remove its build script, patch, profiles, and lock from the production repository while retaining the negative result in history.
- [x] Re-audit current MW4 community recipes and maintained compatibility projects; identify dgVoodoo2 as the convergent Windows/Lutris path, record exact 2.86.5 archive and x86 DLL hashes/terms, pass Defender, and complete bounded three-title load smokes.
- [x] Use an exact-hash dgVoodoo2 import/profile and preserve a collision-safe, rollback-capable owned-install migration path that removes the exact old DDrawCompat profile names from existing installs.
- [x] Reproduce all 13 reviewed Vengeance InstallShield long-name mappings, including `Burnloop_lr_15.avi`, and route existing verified installs through the ownership-safe migration transaction so Vengeance and Black Knight can cross the shared intro-to-menu asset boundary.
- [x] Interactively qualify setup `0.6.22` through original startup movies, menus, pilot creation, and playable game entry for all three titles on the current NVIDIA/Windows system.
- [ ] Qualify the new presentation profile through the same paths plus non-minimizing Alt-Tab and return, screenshot layering, and one retained save/load cycle per title.
- [ ] Field-prove the elevated setup's exact executable firewall pairs for both installer choices and uninstall. A 2026-09-24 silent in-place C: reinstall with private access selected succeeded and logged Private-allow/Public-block rules for all three exact installed executables; effective ActiveStore readback showed all six enabled rules. Launcher-started Vengeance reached its main menu and Black Knight reached its roster screen without a Windows Security prompt. Computer Use stopped before a Mercenaries launch; the Private-declined path, first-start Mercenaries observation, and rule removal on uninstall remain open. Silent installs must remain private-access opt-in, and only project-named rules may be removed.
- [x] Attribute the supplied 23:01 first-start Windows Security screenshot to the concurrent disposable `.local/dinputto8-smoke` Vengeance process, whose executable path is outside the installer-managed C: rule targets. Subsequent normal C: Vengeance and Black Knight launches reached interactive screens without that prompt after the in-place reinstall; the remaining firewall lifecycle gates are recorded above.
- [ ] Verify Black Knight startup video, all in-engine cinematics, and real Alt-Tab return using the final title-specific package.
- [ ] Add only evidence-backed compatibility changes with disable/rollback paths.
- [ ] Qualify or reject official `dinputto8` v1.1.100.0 on the exact final all-title trees. Black Knight loads it without `/gosNoJoystick`, but pilot creation, two consecutive missions, in-mission rebinding, joystick/HOTAS, keyboard/mouse, and the upstream MW4 crash report remain open; do not package it yet.
- [x] Prove a non-elevating, disc-free Black Knight process/window launch after reproducing the official shared Vengeance/`MW4X` topology and Black Knight PR1 state. Dialog-aware menu/gameplay confirmation remains open.
- [x] Re-evaluate the locally patched Black Knight compatibility bundle against fresh officially patched reference trees before restoring only the exact PR1 path to installation policy.
- [ ] Replace timed responsive-process checks with dialog-aware menu and gameplay assertions for all three titles.
- [ ] Qualify fresh Vengeance and Mercenaries media-only coordinator installs through setup-state parity, menu/gameplay checks, exact verification, Defender scan, and ownership-safe uninstall.
- [ ] Test representative Intel/AMD/NVIDIA systems and current supported Windows versions.

## 6. Release and operations

Outcome: a clean machine can install, launch, repair, and uninstall a trustworthy package.

- [ ] Build a byte-reproducible per-user setup after the revised setup-only Black Knight capture/static transform passes full worker cleanup, Defender, repair/uninstall, and interactive menu/gameplay gates; the superseded launch-time loader is removed.
- [ ] Smoke every available game/pack/media combination.
- [x] Preserve saves/configuration through both ownership-safe game removal and standard shell-package uninstall smoke paths.
- [x] Permit reinstall over manifestless residue preserved by safe uninstall without deleting user data; reject owned-path collisions without mutation and retain setup failure logs after the wizard closes.
- [x] Emit and verify the setup checksum while enforcing payload and release-tree allowlists.
- [x] Scan the current setup and qualified Vengeance, Black Knight, and Mercenaries trees with current Defender intelligence.
- [ ] Document signing, SmartScreen, rollback, support bundles, and coverage gaps.

## Next slice

```text
User-facing goal: preserve the proven all-playable baseline while correcting presentation and making validated Mech Paks genuinely selectable.
Owning boundary: exact manual geometry, dgVoodoo aspect/upscale profile, exact-hash archive flags, post-recognition executable ownership gate, and field-visible game behavior.
Smallest verifiable slice: package the post-`0.6.33` opening correction, confirm strict 4:3 outside the sole Vengeance live-action segment, then field-capture an actual mission to judge the five-pixel top/left seam crop.
Focused evidence: the embedded Microsoft/FASA splash is centered and uncropped at 4:3; only the later 30:17 live-action segment covers the final display target without stretching; menus and all other videos remain 4:3; an actual mission capture has no top/left seam; installed profiles report `FullScreenMode=false`, centered `borderless, fullscreensize`, `stretched_ar`, `Resolution=unforced`, `ExtraEnumeratedResolutions=max_4_3`, 4x MSAA, 16x filtering, and both watermark families disabled.
Smoke/regression evidence: `0.6.23` was field-rejected for a 342-point manual crop, global 16:9 stretching, and table-visible but PID-gated pack chassis. `0.6.24` was blocked at the real destination by dead project registration values. `0.6.25` fixed stale-registration replacement but was field-rejected for global stretch, incomplete pack ownership, and minimizing Alt-Tab. The source correction now passes the presentation contract, synthetic selected/unselected pack tests, and exact transforms of all three real final executables; interactive proof remains open.
Rollback or disable path: retain the last title-specific DDrawCompat payloads only as A/B evidence; never restore `/gosnovideo` as a release default or introduce injectors, opaque no-CD files, codec packs, or Defender exclusions.
Hardware coverage gap: only this NVIDIA/Windows multi-monitor device is available; gameplay, input, audio completeness, saves, multiplayer, and additional GPUs remain unqualified.
```
