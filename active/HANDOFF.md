# Session handoff

Updated: 2026-09-24.

## Current state

- Corrected `0.6.46` Settings candidate (2026-09-24): the user identified that the first preview dropdown omitted ordinary monitor sizes. The source now includes 1600x900, 1920x1080, 2560x1440, and 3840x2160 16:9 previews; 1920x1200 and 2560x1600 16:10 previews; and the three ultrawide previews. The current Windows monitor remains the launch mode. Changing a preview no longer changes the displayed **launch render** value, which is calculated from that current monitor. Source-form renders for 1920x1080 and 3440x1440 were visually checked; core smoke and all six PowerShell contracts pass. The first `0.6.46` installer did complete an in-place C: install from all five ZIPs: worker ended `All selected games installed and verified`, the launcher reported three Ready games and both packs, effective C: Private-allow/Public-block firewall pairs were present, and the installed tree passed Defender. That first package's GitHub draft was deleted before publication; public Latest remains `0.6.45.9012`. The **corrected** setup was built twice from declared sources, byte-identical at 174,495,926 bytes and SHA-256 `218e93225ea85c4afcac1a2e9995d98eb6a5d0354b191105bc61726a82640574`. Corrected setup and staged payload passed release-tree policy and Defender engine `1.1.26080.3`, intelligence `1.459.366.0`. A second C: in-place install of these exact corrected bytes was launched and is waiting on UAC; after approval, verify installed UI, game launch, and installed-tree scan before publication.

- Release candidate `0.6.46` (2026-09-24): the #7 guard now sizes the **client** area to the current physical monitor rather than shrinking the outer frame to it. A synthetic framed-window case with the reporter's +6/+29 outer difference and a borderless case both pass against the real guard. The #8 guard no longer overwrites a game's existing cursor clip every 25 ms; synthetic ownership/disposal tests pass. The source-built gameplay-only seam trim is reduced from five to two 800x600 source pixels, leaving menus and films unchanged. The Settings dialog adds 2560x1080, 3440x1440, and 5120x2160 **layout previews**, showing their centered 4:3 areas and pillars; launch always uses the monitor's current Windows mode. The three exact MW4 render choices remain 800x600, 1024x768, and 1600x1200 for HUD safety. Source builds, focused contracts, a desktop UI preview render, and two independent identical add-on builds pass. The full 174,486,938-byte setup built from declared sources at SHA-256 `dfe4dc824c4115bf1c0cef734cf56d8c3fd71f5ce97f98dfd6b0313130296efb`; Defender engine `1.1.26080.3`, intelligence `1.459.366.0`, found no target detections. A C: in-place setup smoke, installed-game regression, and ultrawide/reporter hardware checks are still pending at this point. Do not describe #7 or #8 as confirmed on the affected systems.

- Earlier release staging snapshot (superseded by corrected Settings build): two independent `0.6.46` setup builds had the same SHA-256 above. All focused PowerShell contracts and core smoke passed after the Settings label assertion was updated. Source commit `903a9b3` was pushed to `main`. A GitHub **draft** `v0.6.46` temporarily contained that earlier installer and checksum asset; GitHub reports the installer digest `sha256:dfe4dc824c4115bf1c0cef734cf56d8c3fd71f5ce97f98dfd6b0313130296efb` and 174,486,938 bytes. The public Latest remains `v0.6.45.9012`. The in-place C: silent setup command is waiting on Windows UAC before any worker run or install log. An attempted Computer Use inspection of the UAC window was rejected by automatic safety review; do not retry through another path. The user needs to approve the visible UAC prompt before the installed smoke gate and public release-link commit/publication can finish. The intended README and v0.6.46 release notes are local, uncommitted changes until then.

- The obsolete `F:\Mechwarrior Test\issue8-guard-9013` Inno shell was removed by its exact uninstaller after the user finished the Mercenaries mission test. The C: game installation and registration remained intact. Its public-desktop shortcut was removed by that uninstaller; a working per-user desktop shortcut was restored to the C: launcher.

- GitHub issue intake (2026-09-24): [#8](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/8) reports Mercenaries mouse-torso control snapping down/right and becoming unresponsive on `0.6.45.9012`; inverting control snaps upward, while numpad torso input reportedly works. This is mission input failure, distinct from the earlier initial cursor-placement report. [#7](https://github.com/Icehellionx/MechWarrior-4-Remastered/issues/7) has a new `0.6.45.9012` screenshot showing remaining gameplay HUD crop at 5120x2160 after the outer-window pin. Both remain open; inspect mouse/clip/client coordinate ownership and borderless client/final image geometry respectively before a release claim. The production plan records exact A/B and affected-hardware gates.

- #7/#8 diagnostic slice (2026-09-24): `WindowLifecycleProbe` now serializes the game client rectangle in screen coordinates, DWM extended frame bounds when available, cursor position, window DPI, and DPI awareness beside its existing outer window/clip snapshots. It builds with no warnings but has not been run on the affected machines. The current 5120x2160 geometry calculation already yields 2880x2160 for the 4:3 display area; MW4 still renders at a safe selectable mode (up to 1600x1200), so changing a resolution value alone is not a qualified crop fix. Source survey and ordered A/B slices are in `INPUT_DISPLAY_RESEARCH.md` and `PRODUCTION_PLAN.md`.

- #8 candidate source correction (2026-09-24): `SystemGameWindowLifecycleGuard` no longer reapplies monitor-wide `ClipCursor` every 25 ms over dgVoodoo/game capture or clears the global clip unconditionally at disposal. It claims only an unrestricted cursor once per activation on a multi-monitor desktop and releases only its own unchanged clip. A foreground synthetic WinForms smoke using the real guard passed: a tighter simulated wrapper clip persisted while active and after guard disposal. Core smoke, launcher build, presentation, user-flow, packaging, and release-tree tests pass. A source-built internal `0.6.45.9013` payload and uniquely identified setup (ignored `.local/issue8-stage-20260924`, `.local/issue8-package`) passed release-tree policy and Defender engine `1.1.26080.3`/intelligence `1.459.366.0` with no detections. The user approved its UAC, but the worker correctly rejected a second Mercenaries install because the desktop user's registered C: executable is still present. Inno had already installed the uniquely identified setup shell at `F:\Mechwarrior Test\issue8-guard-9013` before its post-install worker rejected the conflict; the shell needs its own uninstall/cleanup after the current game test. No F: game mission or reporter-hardware confirmation has occurred. This candidate is not published.

- #8 diagnostic component smoke (2026-09-24): source-only `InputCoordinateProbe` compiles and rejects a no-game desktop. A 25-second synthetic process named `MW4Mercs.exe` was launched under the desktop user; the probe captured 92 read-only snapshots for a ten-second request, reporting the expected 2560x1440 monitor, 96 DPI/unaware synthetic window, and all samples foreground. Its JSON is in the user's temp directory and contains no game files or private paths. A real MW4 mission sample remains necessary.

- Existing C: Mercenaries baseline (2026-09-24): the registered per-user game at `C:\Users\icehe\AppData\Local\Programs\MechWarrior 4 Remastered\mercenaries` launched from its existing launcher without modifying that installation. The first ten-second roster probe captured 92 foreground samples with 2560x1440 outer/client/monitor, monitor-wide clip, 96 DPI, DPI awareness 0, and cursor at 799,599. During user-controlled testing, a later 30-second trace captured 274 foreground samples with the cursor ranging x=600–841/y=575–612 and returning to 799,599, 272 monitor-wide and two virtual-desktop clip samples. The user reports no down/right drift on this machine. This does not test the new guard in a mission or reproduce issue #8's reporter hardware. Computer control detected user input and stopped automated game actions.

- Regression follow-up (2026-09-24): the widened cursor/window component smoke also modeled the issue #7 +6/+29 outer oversize on the present 2560x1440 monitor and passed: outer and client rects were pinned to 2560x1440 without disturbing a tighter wrapper clip. The core smoke including 5120x2160/3440x1440 4:3 geometry assertions, launcher build, presentation, user flow, packaging, privilege, compatibility-source, and release-tree contracts pass. A separate desktop-user run of the core suite with `MW4_CROSS_VOLUME_SMOKE_ROOT=F:\Mechwarrior Test` passed; an initial attempt needed a restore under the desktop user's NuGet cache because sandbox-owned assets pointed at an inaccessible cache. No MW4 gameplay or affected ultrawide display was involved.

- Public v0.6.45.9012 (2026-09-24): source commit `54780180313ff3d65ba78b3cc255b09dbb4b3fc0` and installer SHA-256 `99308010a9f824c6c2642be1188ef62fd2e72a60f18b483d459945488b8ebdcd` are live as GitHub's Latest release. A second clean package build was byte-identical. The exact setup, staged payload, and F: installed tree passed Defender engine `1.1.26080.3`, intelligence `1.459.366.0`; GitHub asset digest and size match locally. README links the new installer directly and keeps v0.6.44 as the legacy fallback. The next release must address the remaining gameplay, cursor, media-revision, private-declined firewall, and uninstall gates described below.

- F: packaged field run (2026-09-24): current-source `0.6.45.9012` built from declared sources; setup SHA-256 `99308010a9f824c6c2642be1188ef62fd2e72a60f18b483d459945488b8ebdcd` in ignored `.local/package-safe-resolution-20260924`. The user approved UAC and silent setup installed all three games and both Mech Paks from the five known ZIPs at `F:\Mechwarrior Test`; the setup log says all selected games installed and verified. `netsh advfirewall` readback showed exact F: executable paths under enabled Private-allow/Public-block pairs for Vengeance, Black Knight, and Mercenaries. Launched all three from the installed launcher in the desktop user's session: Vengeance reached pilot creation at the configured 1600x1200, Black Knight reached pilot creation, and Mercenaries reached its roster. No Windows Security firewall dialog appeared on any first launch. Black Knight's pointer was observed near screen center during its opening cinematic; Vengeance's pointer was also inside the game at pilot creation, so the reported bottom-right initial placement was not reproduced here. A pilot/mission was not completed in this run: remote click/activation was inconsistent in Vengeance, and user input was detected during Mercenaries control, so further interaction stopped. This is stronger firewall and F:-install evidence, but does not close alternate-media, all-mode gameplay, cursor drift, or private-declined/uninstall tests.

- HUD resolution root cause (2026-09-24): disassembly of the retained exact Vengeance, Black Knight, and Mercenaries executables found the same `HUDNumberText::GetIndex` height dispatch. It accepts only heights 480, 600, 768, 1024, and 1200; all other heights reach the observed `STOP: unknown screen size` path. Consequently 1280x960 and 1920x1440 are unsafe too, despite prior menu-only evidence. Current source lists only 800x600, 1024x768, and 1600x1200, and the pinned dgVoodoo profile enumerates only 1600x1200 beyond stock modes (SHA-256 `7ea9e4576a421157927de2d41551e3fdef8b4c76cd3adcc2d02ef19706f249a4`). A scratch-only exact-hash branch probe was made in ignored `.local/hud-resolution`, never in product source. Its requested 1200x900 launch entered Training, but the game rewrote `options.ini` height to 600 (the width varied between 800 and 1200 during/after the run), so it does **not** qualify native 1200x900 or the prototype. The scratch process exited and its temporary user registration was restored. Before enabling any nonstandard height, prove actual in-game render size, HUD layout, pointer mapping, and missions across all three exact executables. The source safety correction passes focused tests; a fresh packaged gameplay run remains open. The user's bottom-right startup mouse position remains a separate unqualified input defect.

- Superseded preset field correction (2026-09-24): isolated F: `0.6.45.9011` installed Vengeance, Black Knight, Mercenaries, and both packs. Vengeance crashed entering Basic Training at both `1200x900` and `1440x1080` with `STOP: unknown screen size in HUDNumberText::GetIndex`; the 1200x900 menu also mis-mapped the pointer. The original F: and C: test installs were subsequently removed by the user. The first source correction still retained 1280x960 and 1920x1440, which the later disassembly above invalidated. Do not distribute `.9011` or infer gameplay support from its menus.

- Superseded preset proposal (2026-09-24): the earlier source added 1200x900, 1440x1080, and 2880x2160 based on geometry alone. The subsequent F: install and mission crashes above invalidate its gameplay claim. Its retained `.9011` installer is test-only and must not be distributed.

- RTX 5090 DirectDraw triage (2026-09-24): added source-only `tools/compatibility/DirectDrawProbe` as a self-contained x86 startup probe. It loads either Windows' x86 DDraw or a caller-selected exact game-local DLL and reports SHA-256, enumeration HRESULT/device count, and `DirectDrawCreateEx(NULL, IID_IDirectDraw7)` HRESULT/object state. An optional `surface` stage uses a hidden window for `SetCooperativeLevel(DDSCL_NORMAL)` and primary-surface creation without changing display mode or installed games. Internal publish used existing restore with NuGet audit disabled solely because the vulnerability feed was unreachable; it is not a release artifact. On this non-5090 PC, system DDraw and the Vengeance, Black Knight, and Mercenaries wrappers in the known F: test install all returned `0x00000000` for all four calls and created objects. Each F: wrapper, D3DImm, and config exact-matched the pinned hashes. Isolated D3D12, D3D11, and D3D11 WARP config copies also passed. So did a deliberately invalid OutputAPI config: even the surface stage does **not** initialize or validate the selected renderer. The affected GitHub issue page could not be fetched in this session; the local production-plan summary remains the reporter evidence. The 5090 driver's behavior and later game init/null-call path remain untested; no backend was changed.

- C: reinstall/firewall field follow-up (2026-09-24): internal `0.6.45.9010` silent in-place setup completed successfully from the five known ZIPs with Private access selected. Worker reported all selected games installed and verified; setup log recorded Private allow/Public block rule creation for Vengeance, Black Knight, and Mercenaries, and effective ActiveStore readback showed the six enabled exact-path rules. The installed launcher showed all three games and both packs Ready. Vengeance reached its menu after original intro and Black Knight reached roster without a Windows Security prompt. Computer Use stopped before Mercenaries could be opened. Private-declined installation and rule removal on uninstall remain unqualified. This newer entry supersedes the older pending-UAC description below.

- Gameplay-only widescreen request (2026-09-24): user requires a reversible Settings checkbox that extends only the live 3D world into the pillar areas, leaves HUD/menus/cockpit framing central and unchanged, and keeps all opening/closing, pre-mission, and in-engine cinematics at authored 4:3. No checkbox or renderer change was added: the pinned dgVoodoo observer sees Direct3D-device lifetime and final presentation but has no camera/cinema state, and its present hook cannot create new world geometry. The exact three game executable backups contain `CinemaState`, `cinemastart`, `cinemaend`, `setcameraFOV`, and `setactivecamera` strings as candidate state anchors; these have not been qualified as runtime transitions. WSGF's tested MekTek MP3.1 wide mode widens real-time cutscenes and stretches the HUD, violating this requirement and not proving behavior of our retail builds. Production plan and research now explicitly require true horizontal 3D expansion, unchanged overlays, reliable cinema gating, and 16:9/16:10 field comparison before exposing the checkbox. No install was attempted.

- Settings expansion source slice: `GraphicsSettingsForm` opens immediately with a disabled Apply button while it asynchronously reads the cached ready status/profile, and `MainForm` gates the entire open/apply lifecycle so repeated clicks cannot create multiple dialogs. It offers 4:3 game render resolution with automatic monitor-fitting default, dgVoodoo resampling, DirectX texture filtering (1/2/4/8/16), and antialiasing (off/2x/4x/8x); the shown displayed 4:3 surface and pillar/letter bars remain tied to the physical monitor. The later preset correction above supersedes the original monitor-clamped resolution selection. Profile permutations are generated only from the pinned default and all-title replacement retains ownership/rollback. Launcher build, core smoke, user-flow and presentation contracts pass. Packaged gameplay behavior at non-default settings remains open.

- C: firewall reinstall attempt: user authorized an in-place reinstall to check first-launch prompts. Current-source core/packaging/user-flow/presentation/privilege checks passed; a fresh offline-restored internal stage `.local/stage-c-firewall-reinstall-20260923` passed release-tree policy and Defender. Inno 7.1 compiled `.local/package-c-firewall-reinstall-20260923/MechWarrior-4-Remastered-Setup-0.6.45.9010.exe` (174,496,972 bytes, SHA-256 `1705a6f0f1c949305a8a8dff867cb57ed041ed776e9a7eed7df1ebf565e7ebe7`), also Defender clean. Original C: executables, manifests, four options files, and worker log were copied into ignored `.local/c-reinstall-firewall-20260923` with hashes; desktop-user game registration exported there, and six effective firewall rules snapshot there. All three C: games and both packs returned Ready in 2.61s under a new source read-only status probe. The old installed launcher opened but remained on CHECKING INSTALLATION for over a minute; it was closed. Computer Use launch of the new setup timed out at UAC before the installer appeared, leaving C: unchanged. An async request asks the user to approve UAC; do not claim reinstall or prompt-free launches until setup runs. The stage used `-UseExistingRestore` with NuGet audit disabled only for internal verification, so no release qualification follows.
- Screenshot follow-up: the user supplied a 23:01 Windows Security dialog asking whether to allow public/private network access for “MechWarrior IV.” The disposable `.local/dinputto8-smoke/vengeance-current/MW4.exe` process launched at 23:00:53, remained running after Computer Use stopped, and is outside every project-owned C: firewall rule's exact program path. All six C: rules were read back from the effective `ActiveStore` as enabled Private allow/Public block pairs targeting the normal installed executable paths. Thus this screenshot is consistent with the scratch launch, not evidence that C: installer rules failed. The scratch process had exited by the time controlled cleanup retried; no live C: process was stopped. The user was asked whether normal C: launches also prompt. The no-private-multiplayer source change remains unfielded.
- Firewall follow-through: user clarified the first-listener Windows Defender Firewall prompts came from the isolated F: test copy, which had intentionally been installed with `/ALLOWPRIVATEFIREWALL=0` and without firewall cleanup to protect the live C: installation. The C: production copy already has exact-path Private-allow/Public-block pairs for all three games; the active network is Public. Source now gives the installer a complete decision in either choice: Private allow when multiplayer is selected, Private block when declined, and Public block always. Packaging/user-flow contracts and core smoke passed; Inno 7.1 compiled this code against an existing internal stage. The resulting ignored `.local/firewall-package-20260923` setup SHA-256 `c6af05174d4b5c24dd0cfe0598b8f48053786a87b9c79d35ef88bef675c6ff17` is a syntax/check artifact, not a newly rebuilt or qualified release. Elevated add/query/delete, prompt-free game launch, and isolated F: rule lifecycle are still field gates; do not run a second install using production rule names while C: is active.
- Exact installed-binary input inspection: all three F: game executables contain `dinput.dll` and `DirectInputCreate(Ex)` strings and dynamically import `LoadLibraryA`/`GetProcAddress`, while none statically imports `dinput.dll`. This supports a legacy DirectInput adapter as the next measured input boundary; it does not prove executed joystick calls or multi-device behavior. The current machine exposes no named HOTAS/HID game controller and reports 96 DPI, so hardware and >100%-scaling behavior remain untested.
- Input/display follow-through: `InstallStatusReader.Read()` now caches owned-file verification per physical root within one refresh, so the shared Vengeance/BK/pack tree is hashed once instead of four times. Core smoke and launcher build pass; the F: known install returned all three game states Ready in 3.14 seconds. The source-built dgVoodoo add-on builder now supports an explicit test-only 0/1/2/5-source-pixel crop parameter without changing the production patch; all four x86 variants compiled, and the 5-pixel binary/patch hashes exactly match the pinned production outputs. Visual capture through Windows Computer Use failed with GPU device suspended (0x887A0005), so no variant is gameplay-qualified. The F: launcher process was closed after its status screen did not settle during this attempt; temporary F: HKCU registrations were reimported from fresh C: exports, all three paths verified, and the 30-minute fallback watchdog disarmed. No HOTAS device was present in the local HID inventory and current Windows scaling reported 96 DPI, so multi-device and non-100%-DPI repros remain open.
- Input/display research: [the 2026-09-23 source survey](INPUT_DISPLAY_RESEARCH.md) maps DPI-related down/right drift, dgVoodoo cursor/GDI-hook history, keyboard/mouse rebinding, `dinputto8` second-mission and in-mission-binding risks, one-logical-device HOTAS routes, retail-versus-MekTek widescreen evidence, and the shipped five-source-pixel gameplay crop. The current profile already uses Dege's recommended cursor-only hook and a 30 FPS cap. The production plan now has separate input/HOTAS, optional gameplay-only widescreen, and edge-preservation qualification gates. No new DLL, driver, game transform, or widescreen setting was shipped from this research.
- Current-source CUE/F: packaged check: isolated test-only `0.6.45.9008` installed Vengeance, Black Knight, Mercenaries, and both Mech Paks into `F:\Mechwarrior Test\app-cue` from four supplied ZIPs and a CUE/MODE1/2352 BIN regenerated from the supplied Inner Sphere ISO. The worker recognized all five F: sources, applied the official game updates, exact-verified both game trees, prepared graphics and presentation, and ended `All selected games installed and verified.` All three F: executables opened their titled windows with production launcher arguments and were closed. The three temporary F: HKCU game registrations were restored from fresh exports to the existing C: executables, verified, and the restore watchdog was disarmed. Setup SHA-256 `B0D6AD1E43A53714EFF713F02E8CE45C328DEE11F2CD3BE9E287A6D1C30D1E73`; worker log retained at ignored `.local/f-cue-packaged-success-worker-20260923.log`. This proves the current CUE-capable package can ingest a known-edition CUE/BIN and install/open from F: on this host; it does not qualify a different disc revision, gameplay, uninstall, or release. The isolated F: app remains, but shared game registrations now correctly point to C:.
- Candidate-media work: the 29 MW4 CUE sheets (16 Vengeance, 3 Black Knight, 7 Mercenaries, 3 packs) each name one same-directory MODE1/2352 BIN, but none of the referenced BINs is present; the other 2 CUEs are MW2 and out of scope. Source-only `CueMode1DataStream` now validates a single index-zero track, rejects missing/escaped/reparse/corrupt BINs, and exposes 2048-byte logical sectors. `MediaSourceSessionFactory` projects Mech Paks directly from that stream and converts other discs into a cleaned, owned temporary ISO before existing mounting/recognition. The wizard accepts CUE selection. Synthetic sector, recognition, pack exclusion, rejection, cleanup, real known Inner Sphere ISO-to-raw-BIN round-trip, core smoke, installer/launcher builds, package/user-flow contracts, and Inno syntax compile against a prior payload passed. No alternate game-file revision has been qualified; current-source packaged CUE intake passed with known-edition bytes, and official patch/executable hash gates remain. The CUE files and media remain outside the repository.
- Latest non-C: user-approved, backed-up temporary redirection of three desktop-user HKCU game `EXE Path` values allowed isolated test-only `0.6.45.9006` to install from five SHA-256-verified ZIPs on `F:\Mechwarrior Test\media` into `F:\Mechwarrior Test\app`. The worker applied official Vengeance Patch 3, Black Knight PR1, Mercenaries PR1, and both Mech Pak overlays; both physical game trees committed and exact-verified, graphics and legacy registrations completed, and the worker ended with `All selected games installed and verified.` All three F: game executables then opened titled windows using the production launcher arguments (Vengeance PID 13380, Black Knight 65504, Mercenaries 56932), and each smoke process was closed. Original C: registry exports were reimported, all three C: `EXE Path` values were verified against existing files, and the 15-minute fallback watchdog was disarmed. The F: test app and five media copies remain for inspection; its launcher will conflict with restored C: registration until this disposable test app is removed or isolated again. The successful worker log is retained under ignored `.local/f-clean-success-worker-20260923.log`. This proves the supplied media can install and open windows from F: on this host, but does not reproduce other machines, full gameplay, the package uninstaller, or new media revisions.
- Follow-up on the non-C: issue: the installer now distinguishes a live registration for another copy from all other worker failures. The read-only preflight throws a specific conflict exception, the worker returns a dedicated code, and Inno displays an actionable explanation while preserving its diagnostic log. An ignored probe against the desktop user's live C: registrations confirmed Vengeance, Black Knight, and Mercenaries all take the specific path for F: targets. Core smoke, installer/launcher builds, all focused contracts, Inno compilation, release-tree policy, and Defender scan pass for internal `0.6.45.9004` (174,488,897 bytes, SHA-256 `AEB9920F789153F42D6BF8F769C12F0574CD85DB4EBED6F6BB906C39E1300BEC`); no new packaged install was run. The proven F: failure was a second-install registration conflict after both game trees exact-verified on F:, while a clean-user F: field run remains open. The user emptied the isolated F: directory; its unique test uninstall registry entry is stale and medium-integrity removal was denied.
- Real non-C: packaged test on the user-designated empty NTFS `F:\Mechwarrior Test` with five supplied ZIPs on `C:` used an isolated AppId/Start Menu identity and no production firewall cleanup. Both physical game trees, including Black Knight and both Mech Paks, exact-verified on `F:`. The worker then failed at late legacy registration because the desktop user's C: Vengeance executable is still registered and present; it rolled back both game trees, retaining only four setup-created `options*.ini` files. The earlier apparent absence of keys was from `codexsandboxoffline` HKCU, not the desktop user. A read-only check under the actual `icehe` token confirmed all three registrations still point at C:. Source now preflights every planned game registration read-only before game-file mutation and retains the final conflict check. An ignored, built read-only probe run under `icehe` verified the preflight rejects all three live C: registrations for planned F: targets. Core smoke, installer/launcher builds, user-flow, packaging, presentation, privilege, compatibility-source, release-tree policy, and diff checks passed after the change. The updated 26-file offline-restored internal stage at `.local/stage-issues-preflight-20260923` passed release-tree policy and Defender without detection; online NuGet audit remains required for release. The user manually emptied `F:\Mechwarrior Test`; child count is zero and no UAC/test process remains. The isolated test AppId still has an HKLM uninstall entry pointing at the deleted shell. Its exact identity/location were verified, but medium-integrity removal was denied; remove only that stale test entry through an elevated path when available. The C: production AppId and all three game registrations remain untouched. The ignored worker log was retained at `.local/f-packaged-install-worker-20260923.log`. Alternate retail revisions are cataloged at Redump/BetaArchive, but no third-party full-disc archive was downloaded because redistribution permission was not established. A clean-user F: install and affected reporter hardware/media remain untested.
- The user's five newly supplied ISO ZIPs were hash-compared against the existing ignored media fixtures; all five are byte-identical, so they add no new media-revision coverage. A non-admin Vengeance ZIP probe exposed raw PowerShell CLIXML on `Get-DiskImage` access denial. The backend now emits a path-free UAC/action diagnostic, with focused tests; both Mech Pak ZIPs recognize directly. The Vengeance-only visual fixture exposed a launcher state bug: an absent Black Knight expansion was shown as repair-required and disabled SETTINGS. Source now shows it as Not Installed, keeps genuinely damaged declared expansions repair-required, and enables SETTINGS. The 2560x1440 dialog was visually checked: it reports a 1920x1440 gameplay surface and 320px side bars; a UI-driven Soft change and Default reset preserved manifest validity and exact default bytes. The SETTINGS caption is centered. The latest synthetic fixed-F: cross-volume core smoke and all source contracts pass. A Ready C: install remains untouched. A second packaged F: install on this machine risks changing shared, currently consumed title registration; it is deferred to a disposable Windows environment or an ownership-safe isolation protocol. This is not evidence of the reported non-C: root cause. Internal setup `0.6.45.9002` includes the final status and registration-message fixes, compiled from the latest 26-file stage, passed release-tree policy, and produced no Defender threat for its exact 174,476,927-byte executable (SHA-256 `07325847969B52F7951A94358A58E0088E2E8795DE250FDD09A60A4CB50033BB`). It is not a release candidate. Online NuGet vulnerability audit is blocked by network access; local offline restores were used only for verification. The unrelated pre-existing core `packages.lock.json` change remains untouched.
- Post-`0.6.44` public-issue work has no qualified package or new release. A fresh local retail Disc 1 plus Inner Sphere Patch 3 transform passed; all 11 retained output paths and SHA-256 values matched the prior qualified reference, and no server shortcut entered the payload. The transform now accepts only the exact known 25-byte optional server shortcut in scratch and reports actual size/hash on mismatch; the original-disc reporter variant remains untested. The launch guard now pins physical monitor bounds after borderless stabilization, pending the reported 5120x2160 visual test. ISO query diagnostics distinguish empty output, externally mounted ISOs give eject/retry guidance, and near-match media lists missing expected files without accepting an unknown edition. Black Knight-only selection explains its Vengeance and Mech Pak dependencies. A source-only SETTINGS dialog shows exact monitor/gameplay/bar geometry and offers bounded final resampling with manifest-owned all-title replacement and rollback. A source-only DIAGNOSTICS dialog copies a path-free, read-only report of title states and verified executable/graphics hashes. Synthetic ready/damaged, three-title, rollback, save, uninstall, media near-match, and geometry tests pass. A separate synthetic fixed-`F:` destination smoke passed with source and scratch on `C:`, but the reported packaged non-C: failure remains unreproduced. Two internal stage-only payloads under ignored `.local/stage-issue-slices-20260923*` contain 26 byte-identical files and passed release-tree policy; the first passed Defender engine `1.1.26080.3`, intelligence `1.459.343.0`, with no new detection. NuGet's online vulnerability feed was unavailable, so these stages explicitly reused existing restored packages; a releasable setup build still requires a fresh successful audit. Core smoke, installer/launcher builds, packaging, presentation, privilege, user-flow, and release-tree policy checks pass; setup compilation, full install/uninstall, affected GPU/storage, alternate-media, and packaged visual/field gates remain open. The unrelated pre-existing `src/MW4Remastered.Core/packages.lock.json` worktree change was left in place.
- Public release `0.6.44` fixes the public `0.6.43` elevation failure. The outer Inno loader retains `PrivilegesRequired=admin`, but Setup now explicitly starts its hidden worker through `ShellExec('runas', ...)`, and the worker independently rejects a non-administrator token before media inspection. A normal launch with no manual **Run as administrator** action automatically presented UAC, then installed and exact-verified Vengeance, Black Knight, and both Mech Paks, including successful PR1 image capture, ending with `All selected games installed and verified.` The user separately confirmed this exact local installer runs correctly. The disposable game tree and exact registration values were removed through the ownership-aware cleanup path. All seven automated suites and release-tree policy pass. The exact 174,471,026-byte setup has SHA-256 `a3fdde1fbe6265c51c9a15258ea8ec79272a8e6430b9585bb6257275f5fed786` and produced no Defender detection under engine `1.1.26080.3`, intelligence `1.459.273.0`. GitHub re-download verification is recorded with the release publication.
- Public `0.6.43` is field-rejected and must be superseded. Its online asset is byte-identical to the locally qualified setup, but a normal launch allowed the hidden worker to run without the administrative token required by the setup-only Black Knight PR1 capture; all four attempts exited without a mapped image. Explicit **Run as administrator** made the same downloaded bytes succeed, proving download corruption and media differences were not the cause.
- The `0.6.43` graphics baseline keeps the physics-safe 30 FPS cap in both dgVoodoo and the game launch arguments, explicitly enables 4x MSAA and 16x anisotropic filtering, upgrades final display resampling from bilinear to Lanczos-3, and retains dynamic `max_4_3` enumeration with explicit high-resolution 4:3 fallbacks including `1920x1440` and `2880x2160`. This addresses the field crash in which the launcher requested `1920x1440` but MW4's enumerated mode list omitted it. The updated profile SHA-256 is `fd9413ae24ef4028c19b304b48f7062d53871f25942baaa17d554bfeb1261b93`. The exact local and GitHub setup bytes match at 174,462,561 bytes, SHA-256 `860abdfae1281e99209f0a515838eed19fe220dcf4e1a38c4122ce37056edf87`; this graphics configuration carries into `0.6.44` unchanged.
- Public release `0.6.42` recovers from the exact intermittent Black Knight PR1 first-capture failure that field-rejected `0.6.41`. Setup retains suspended creation and job assignment before resume, but now uses up to four isolated 15-second attempts with fresh app-local capture files, processes, owned jobs, and output paths inside the former one-minute bound. A packaged adversarial five-ZIP run deliberately blocked attempt 1 and recorded `SABOTAGED=True` plus completed verification for both game families; the same package then completed the real default-path update with `All selected games installed and verified.` Two clean setups are byte-identical at 174,458,178 bytes, SHA-256 `d5fab3fa2a47d679d9ad8c340749b02861703cfb0d808b571ff1124a2cc8f00e`; all seven automated suites pass, and the exact setup plus complete installed tree passed Defender engine `1.1.26080.3`, intelligence `1.459.273.0`, with no new detection.
- Public `0.6.41` is field-rejected and must not be offered again. The user's exact downloaded package still intermittently produced no mapped image on its single attempt, and its failure diagnostic then obscured that timeout by querying `ExitCode` on a process attached by identifier. An immediate rerun of the same bytes and inputs succeeded, proving the remaining per-attempt initialization failure was intermittent; `0.6.42` removes the invalid diagnostic and retries from a fresh capture environment.
- Public `0.6.40` is field-rejected and must not be offered again. Its 60-second wait did not fix the underlying start-before-job-assignment race; the user's elevated five-ZIP run timed out without producing any mapped image. Subsequent identical runs succeeded, confirming intermittent launch containment rather than invalid media, path, privilege, or Defender interference.
- Public `0.6.39` is field-rejected for clean installation and must not be offered again. Its downloaded bytes exactly matched the qualified local build, and Defender recorded no detection, but a real five-ZIP run stopped while Black Knight's short-lived protected launcher returned code 1 before setup observed its temporary child's mapped image. Its graphics, presentation, and field-confirmed Mech Pak behavior carry into `0.6.40` unchanged.
- `0.6.38` is the field-proven Mech Pak baseline. Stock Cauldron-Born selection no longer raises the component entitlement dialog after its four-table archive correction. Broader all-eight-chassis and campaign/multiplayer coverage remains open.
- `0.6.37` is field-rejected and must not be offered again. Read-only inspection proves it successfully installed the intended executable hashes `62b9958c…`/`8ff3eeb5…`; the continued Cauldron-Born rejection therefore disproved the assumption that the alternate executable local was the last layer and led to the component-table finding above.
- Internal setup `0.6.35` is field-rejected and must not be offered. It retained the top/left gameplay seam and still rejected stock Mech Pak chassis in Instant Action even though MechLab ownership, variant creation, and models worked. Its desktop-shortcut default, reproducibility, release-tree, and Defender evidence remain valid only for those narrower claims.
- Historical `0.6.37` build evidence remains valid only for the exact executable migration and Defender result. It is not a field candidate because component-table activation was incomplete.
- The gameplay seam is field-confirmed resolved by `0.6.36`; carry that presentation code forward unchanged. The crop keys off the active legacy Direct3D-device lifecycle, trims five source pixels from only the top/left, anchors bottom-right, and explicitly excludes menus/startup movies including Vengeance's embedded Microsoft/FASA splash.
- Internal field candidate `0.6.34` is built at `output/0.6.34/MechWarrior-4-Remastered-Setup-0.6.34.exe`, 174,459,154 bytes, SHA-256 `aeef1a9201d6747cb1cbcaf5b144f4953d00417de657c13f89be716b86a91807`. Two clean setup builds are byte-identical and the exact installer passed Defender engine `1.1.26080.3`, intelligence `1.459.267.0`, with no new detection. Exact-window captures on 2560x1440 prove the embedded Microsoft/FASA splash remains centered and uncropped at 4:3 while only the later 30:17 live-action segment covers the final display target without stretching. The user's gameplay test remains required for the top/left seam crop; Black Knight/Mercenaries presentation and additional monitors/GPUs remain open.
- Field setup `0.6.33` is rejected and must not be offered again. Its Alt-Tab behavior improved, but its Vengeance opening exception activated for the entire `GAMEOPEN.MPG`: the embedded Microsoft/FASA splash was enlarged/cropped and the live-action segment was still presented through a 4:3 intermediate. Current source delays the exception until 12.25 seconds and redraws only the proportional 30:17 live-action crop across the complete final target. Exact-window captures on the current 2560x1440 monitor prove the Microsoft/FASA segment remains centered and uncropped at 4:3 and the live-action segment alone fills the screen without pillarboxes or horizontal stretching.
- Setup `0.6.32` is not presentation-qualified. The user's retained 2161x1374 gameplay image proves a two-display-pixel green seam across the top of the 4:3 viewport and a three-display-pixel seam down its left edge. A later five-source-pixel dgVoodoo add-on probe did not remove it because a direct diagnostic launch let Vengeance rewrite `options.ini` from the intended monitor-derived `1920x1440` to `800x600`; the add-on's incorrect high-resolution classifier therefore never ran. The earlier clean-seam claim is withdrawn. Menus and opening video remain clean and must never receive the gameplay crop.
- Alt-Tab remains open for gameplay qualification. A current disposable-launcher startup-presentation probe passed every measured lifecycle check on the present 2560x1440/164 Hz monitor: borderless/non-topmost desktop coverage, unchanged display mode and window rectangle, visible/non-minimized state while focus was elsewhere, one-Alt-Tab return, click return, inactive cursor release, and active recapture. The probe captured only the exact game window and closed its own test processes. Because DirectInput automation could not enter a mission unattended, production qualification still requires the same packaged-launcher matrix during gameplay in all three titles; direct `MW4.exe` launches remain inadmissible.
- Monitor adaptation is implemented in source and awaits multi-monitor field qualification. At launch, the resolution provider reads the physical bounds of the monitor containing the foreground PerMonitorV2 launcher, derives the largest fitting 4:3 surface, and shares the captured result between the launch argument and guarded INI. `1920x1440` is merely the expected result on the present `2560x1440` monitor, never a universal constant. Deterministic geometry cases and the focused presentation contract pass.
- Vengeance's live-action opening is the sole permitted exception to global 4:3 presentation. If and only if the exact opening asset can be identified, proportionally zoom it until pillar bars disappear and letterbox bars are minimized without changing its aspect ratio. Do not alter the media, stretch, crop menus/gameplay/other videos, affect Black Knight or Mercenaries, or use a global ROI.
- Installed setup `0.6.29` is field-rejected and must not be offered again. The real install still switched the physical display mode, stretched the 4:3 game to widescreen, displayed the dgVoodoo watermark, and listed Mech Pak chassis that failed the native ownership check. Its reproducible build, static profile assertions, and Defender result did not prove those behaviors and must not be cited as qualification.
- The current source uses exact-hash dgVoodoo 2.87.5, D3D12 profile SHA-256 `fd9413ae24ef4028c19b304b48f7062d53871f25942baaa17d554bfeb1261b93`, and reproducible add-on DLL SHA-256 `33e7fae1c1cb2d297c05c14b5d4f886676fcd492c44eab0602d8e4465bc71ef0`. The profile forces windowed presentation (`FullScreenMode=false`) in a centered borderless desktop-sized window, keeps aspect-preserving `ScalingMode=stretched_ar`, and disables both watermark families. Ordinary content has occupied a centered 1920x1440 4:3 surface with black pillar bars and no visible watermark on the current 2560x1440 monitor. Current source uses an active-Direct3D-device-only five-source-pixel top/left crop anchored bottom-right; menus and startup movies bypass it, including an explicit pre-live-action state for Vengeance's embedded Microsoft/FASA splash. This remains open until a fresh mission capture proves it. Black Knight/Mercenaries presentation and additional GPUs remain unqualified.
- Mech Pak selection has separate script, model-loading, and stock-roster ownership consumers. Returning success above the loader allowed creation but skipped the model, while `0.6.35` proved that covering the common native initialization still missed Vengeance/Black Knight's alternate Instant Action form. The source now marks only selected, validated pack media present at all five exact native sites in each title while preserving the loader. Synthetic selected/unselected tests and exact transforms of all three real final executables pass. Earlier Vengeance evidence visibly selected an Inner Sphere Highlander and Clan Cauldron-Born with models; fresh stock Instant Action selection in all three titles remains mandatory.
- Field setup `0.6.27` is rejected and must not be offered again. Its selective in-memory Mech Pak projection fixed the `Vigorf.A` setup detection, but the first real launch exposed two independent product defects: `LegacyGameConfiguration` still hard-forced `1024x768x32` and policed that value for 30 seconds, while the dgVoodoo profile used `Resolution=max`; the result remained low-resolution and visibly stretched. Setup also created only Private-profile firewall allow rules while the test PC's active network was Public, so Windows correctly showed its first-listener security prompt. Source now derives the largest monitor-height 4:3 render surface (`1920x1440` on the current 2560x1440 display), sends that exact `-f` request, keeps the guarded INI at the same dimensions, leaves dgVoodoo resolution unforced, advertises `max_4_3`, and forces `stretched_4_3` presentation. Setup now pairs each exact executable's Private allow rule with an explicit Public inbound block rule, rolling back partial pairs and removing both by the same project-owned name. Automated contracts pass; visible gameplay geometry and a prompt-free first launch remain mandatory before handoff.
- Field setup `0.6.26` is rejected and must not be offered. Defender identified the fully materialized temporary Inner Sphere disc image—not the generated game executables—as `Trojan:Win32/Vigorf.A`; cleanup then failed with `0x800700E1`, causing setup to roll back both already verified game trees. Source now reads single-image Mech Pak ZIPs through pinned MIT DiscUtils 0.16.13 in memory and projects only 15 exact allowlisted pack/official-patch files. The legacy installer, autorun, serial, and DRM content is never written, mounted, or executed. The real Inner Sphere projection and a complete five-ZIP Vengeance/Black Knight/Mercenaries/both-pack install passed; the projected tree and complete installed tree produced no Defender detection under engine `1.1.26080.3`, signatures `1.459.256.0`. A stale `0.6.26` temporary ISO remains locked by Windows/Defender until reboot and must not be restored or excluded.
- Rejected `0.6.27` nevertheless retains valid security evidence for its selective pack-media boundary: two clean builds were byte-identical at 173,061,612 bytes and SHA-256 `b58f6ac75c4b5f465582deaf23314ac3899643ea03819194ba1f12bec88807fe`; release-tree policy, Defender, and NuGet audit passed. That evidence does not qualify its presentation or firewall behavior.
- Internal setup `0.6.25` is field-rejected and must not be offered. Although it fixed stale-registration setup failure, the real installed run proved that `Resolution=desktop` stretched every title and screen, the one patched PID-dialog branch did not satisfy the separate script-facing Mech Pak ownership callbacks, and the normal-band borderless window still minimized on Alt-Tab. The intended corrections carried beyond rejected `0.6.26` use dgVoodoo's aspect-preserving `Resolution=max` plus `ScalingMode=stretched_ar`, fake fullscreen as an unqualified Alt-Tab candidate, and `dgVoodooWatermark=false`. Mech Pak activation exact-locates and enables only the selected-media `ShellCheckClanPak`/`ShellCheckISPak` callbacks in addition to the former global dialog branch. Synthetic and all three exact real executable transforms pass, but interactive aspect, Alt-Tab, watermark, and actual chassis-selection proof remain mandatory before qualification.
- Before compilation, two independent private `0.6.26` stages of the source correction produced the same 21 files byte-for-byte and passed release-tree policy. Packaging exact-pins profile SHA-256 `a126cd326ba1e9e96ebedcb7921c535d546933febccf6d7c3b68149a885da90b`; the staged profile explicitly sets `dgVoodooWatermark=false`, `ScalingMode=stretched_ar`, `Resolution=max`, and fake fullscreen. The user's installed `0.6.25` profiles were restored from backup after source testing so its ownership manifest was not left mismatched.
- Two independent compiled `0.6.26` field-candidate builds are byte-identical. Setup is 172,879,541 bytes, SHA-256 `3a6e1addd78ba09fa2dbac6327a2fa7cfd522f220dfaf2d721986efa136f8146`; both release trees passed policy. The exact setup passed a current Defender custom scan with engine `1.1.26080.3`, signatures `1.459.256.0`, antivirus and real-time protection enabled. This is the next interactive test artifact, not a public or gameplay-qualified release. The Vengeance intro remains original and uncropped in this candidate; no global ROI or video-suppression workaround is present.
- Field setup `0.6.22` is the first confirmed all-three-games-playable baseline: Vengeance, Black Knight, and Mercenaries install, play their startup presentations, accept pilot/account creation, and enter playable game state on the current NVIDIA/Windows system. The project has moved from launch recovery to six explicit polish priorities: Vengeance-only 16:9 intro scaling; exact Vengeance manual recrop; native-monitor 3D upscale with maximum safe detail, 4x MSAA, 16x anisotropic filtering, and 32-bit color while preserving 2D; game-visible Inner Sphere/Clan activation; non-minimizing MW3-style Alt-Tab; and complete upstream attribution/credits. Field review rejected the earlier 342-point manual height; all 98 pages now use the cover-derived 509.8 x 323-point canvas. Five contact sheets covering every page show no remaining bottom stripe, clipping, or reordering.
- Internal setup `0.6.24` is superseded because a stale project-owned registration from a removed installation could block setup after all game files were built, and its advertised worker-log path was deleted by rollback. Its manual, renderer, and Mech Pak fixes carry forward unchanged in `0.6.25`.
- Black Knight static v4 now reaches the real menu, accepts pilot creation, and enters playable state. The earlier `PilotEntry.script:455` stop was not an executable, renderer, input, or account-data fault: the retained shared-root `optionsx.ini` contained a CRLF joystick page followed by the launcher's LF-only appended graphics page, so the legacy parser reported `[graphics options] is a required page`. The expansion's own crash report also proved that `MW4X.exe` runs with the shared Vengeance directory as its current directory. Source canonicalizes the complete mutable INI to CRLF, maintains `optionsx.ini` both beside `MW4X.exe` and at the shared runtime root, and explicitly launches Black Knight from that root. Focused regression, all PowerShell contracts, core smoke, launcher/worker builds, two byte-identical 21-file stages, release-tree policy, current Defender scanning, and field gameplay pass. Internal setup `0.6.22` is 172,853,746 bytes, SHA-256 `2ecbf7e9719041b9a150ab8aa280ee00a11ed08ef3dc8c318a8dcbc9bcd08554`.
- Mech Pak activation is source-owned and shared across both physical install families. Exact hashes gate the three qualified archives and final executables. The transform unlocks only selected-media table records, enables only the corresponding shell callbacks, and now projects the selected media into every exact native presence pair without bypassing the loader. Exact per-title site counts fail closed. Vengeance visual proof exists for both pack families; Black Knight and Mercenaries interactive proof remains mandatory.
- The presentation profile now has runtime evidence after the `0.6.29` field rejection. The launcher computes and requests the largest fitting 4:3 surface for the monitor containing the launcher, while dgVoodoo 2.87.5 keeps Windows in desktop mode and presents that surface in a centered borderless desktop-sized window. `Resolution=unforced`, `ExtraEnumeratedResolutions=max_4_3`, `ScalingMode=stretched_4_3`, 4x MSAA, 16x anisotropic filtering, 32-bit SDR, and both watermark suppressions remain explicit. Vengeance visibly held 4:3 with black bars and no watermark without changing 2560x1440; the other titles still require the same proof. The add-on's exact `MW4.exe` plus active `GAMEOPEN.MPG` gate proportionally zoomed only that live-action opening; the preceding startup clip and following menu remained 4:3.
- The Vengeance-only intro investigation has two additional negative results that must not be repeated as release work. A source-built, Defender-clean DDrawCompat experiment exact-gated the existing MW3 center crop on `GAMEOPEN.MPG`, but the final Vengeance Patch 3 executable exited immediately after Direct3D initialization even with the proven baseline profile; it is not a viable renderer replacement. dgVoodoo's public API describes runtime configuration and a D3D12 presentation add-on callback, but the stock 2.86.5 wrapper exports no runtime configuration entry point and did not load the official sample add-on in a D3D12 disposable probe. No project file changed renderer direction from these experiments. External playback followed by a video-suppression launch flag remains rejected by the original-presentation contract.
- Watermark suppression is explicit rather than inferred. The packaged profile and presentation contract require `dgVoodooWatermark=false`, and a read-only check of all three current installed `0.6.25` profiles found the same value. A visible no-watermark run remains required because an absent or ignored profile can still fall back to wrapper defaults.
- Internal setup `0.6.23` is superseded and must not be offered: field review found a residual white strip under every Vengeance manual page, global 16:9 stretching, and Mech Pak chassis that were listed but rejected at selection by the runtime's separate obsolete SafeCast PID check. Its prior build and Defender evidence remains historical only.
- Internal v4 setup `0.6.21` is superseded by the pilot-entry correction. Its prior reproducibility, release-policy, coordinator, and Defender evidence remains valid for the static v4 baseline, but it must not be offered again.
- Black Knight field testing disproved static transform v3 `7761c41d…`: it crashed on frame 1 by jumping into stack memory. Exact-tree A/B ruled out packaging drift and dgVoodoo. Comparison of v3 against the retained structural PR1 oracle exposed 108 incorrect indirect CALL/JMP IAT operands plus two entry-point MOV operands that the 2,990-entry callsite map had misclassified; the two MOVs were both rewritten as `SendMessageA` instead of retaining `InsertMenuItemA` and `EnableMenuItem`. Transform v4 applies an exact-old-slot, exact-RVA, canonical-import correction set of 110 entries before the established 127 tail repairs. It rejects duplicate, unknown, unexpected, or non-code operands and emits deterministic 3,743,744-byte `MW4x.exe` SHA-256 `b31bd051…`. Its `.text` matches the working structural layout except for the two deliberate setup-validation replacements and one stricter related branch. A production-shaped dgVoodoo tree with videos enabled and `-noautoconfigx /gosNoJoystick` remained correctly titled and responsive for 60 seconds. Packaged/focused intro-menu/Alt-Tab/pilot/mission proof remains open.
- Internal setup `0.6.20` is superseded and must not be offered again: its packaged Black Knight v3 executable is field-proven to crash at startup despite its earlier policy and Defender passes. Static v4 subsequently crossed worker/package/Defender and all-three-game field playability in `0.6.22`; repair/uninstall and broader hardware remain open.
- Official `dinputto8` v1.1.100.0/tag `46e7ea7…` is recorded as a separate input candidate, not part of setup `0.6.20`. Exact x86 DLL `f96bb110…` and tagged source ZIP `823ce9b2…` are Zlib-licensed and Defender-clean. The DLL loaded and kept final-static Black Knight correctly titled/responsive for 15 seconds without `/gosNoJoystick`; no pilot/gameplay claim follows. Upstream issue 22 documents Mercenaries second-mission and in-mission binding crashes, and current-stack Vengeance/Mercenaries evidence is still absent. Keep `/gosNoJoystick` and do not package the wrapper until the full interactive input matrix passes.
- Internal field candidate `0.6.19`, built from commit `e925d30`, contains the Vengeance setup-table filename fix and verified-install migration. Setup SHA-256 is `8f010305d46676f82e8b64bae1aa6a358a9e47d9ce8dfdc77464262788eff27c`. Release-tree policy passed, and Defender engine `1.1.26080.3`, intelligence `1.459.256.0`, found no threat or new detection. It is historical Vengeance evidence only and must not be offered again: its Black Knight launch-time DLL route is disproven and superseded by the setup-only capture/static v3 source state.
- The 2026-09-17 field failures after otherwise successful installation are now traced to install-layout parity, not missing codecs. Vengeance completed its original opening movie and then logged `File not found 'Content\Movies\Burnloop_lr_15.avi'` from `PilotEntry.script`; its owned manifest instead contained the ISO's shortened `CONTENT/MOVIES/BURNLO_1.AVI`. Black Knight's valid 31.88-second MPEG-1/MP2 intro likewise stayed responsive until the intro-to-menu boundary and then exited with the engine marker `0xFEEDFACE`. Black Knight supplies no replacement menu loop and shares Vengeance content, so the same missing installed name is the leading common cause. Source now reproduces all 13 reviewed Vengeance source-to-installed-name pairs from the original `SETUP.EXE` table. Existing verified installs receive the same change through the rollback-safe owned-file migration; unowned collisions still fail closed. Core smoke, installer Release build, user-flow contract, and presentation contract pass. A migrated interactive menu/gameplay run remains required before another setup handoff.
- Source and internal stage `0.6.18` now package the exact four dgVoodoo2 x86 DLLs plus the locked MW4 profile for Vengeance, `MW4X`, and Mercenaries. Two clean stages were byte-identical across all 21 files. The stage passed release-tree policy and Defender engine `1.1.26080.3`, intelligence `1.459.256.0`, with no new detection. The exact final staged profile kept all three disposable media-derived executables responsive for 10-second bounded runs with their correct titles. Automated foreground capture was blocked by normal Windows focus-stealing prevention, so startup movies, menus, Alt-Tab, pilot creation, and mission load remain interactive gates; `0.6.18` is not a user handoff.
- Existing verified installs have a tested manifest migration: it may add only the named absent dgVoodoo files, replace already-owned files, retire only the old owned DDrawCompat profile, refuse unowned collisions, and roll back file plus manifest changes on failure. This closes the fresh-install-only gap discovered during staging.
- The 2026-09-17 ecosystem re-audit changes the presentation direction. Current MW4-specific community guidance, Lutris recipes, PCGamingWiki, and modern Windows reports converge on dgVoodoo2; DDrawCompat's own compatibility table still lists MechWarrior 4 as unknown. Exact dgVoodoo2 `2.86.5` archive SHA-256 `76b6893a0be81e3905a03f30f25202d6dc6128c3b8f7a21f2c33bcfabfa75ddf` was obtained from the public preservation mirror after the official tag exposed no binary asset through the GitHub API. Its official terms allow individual files to ship with a game/mod. The complete archive and extracted tree passed Defender engine `1.1.26080.3`, intelligence `1.459.256.0`.
- A disposable A/B tree loaded stock dgVoodoo2 x86 `DDraw.dll` and `D3DImm.dll` in all three media-derived games. Vengeance, Black Knight, and Mercenaries each remained responsive for 35 seconds with the correct title and empty/no crash logs, with startup-video suppression absent. The exact packaged profile later repeated the bounded all-title result. This is candidate evidence, not video/menu/gameplay qualification. It is especially important because Black Knight no longer requires the DDrawCompat-specific native/windowed exception. Next gate: visibly prove every startup movie and menu, Alt-Tab each title, create a pilot, and load one mission before another installer is offered.
- Setups `0.6.16` and locally built `0.6.17` are superseded and must not be handed out: both package the DDrawCompat experiment. `0.6.17` added a tested ownership-safe file-replacement transaction so setup can migrate compatibility files in a verified existing install without touching unowned saves; retain that mechanism for the dgVoodoo migration.
- The first launch of a new executable path can raise Windows Defender Firewall's inbound-listener dialog when the active profile lacks a program decision. `0.6.27` proved that a Private-only allow rule is insufficient when Windows classifies the active network as Public. Current source creates two exact executable-scoped inbound rules under one project-owned name: Private allow if multiplayer was selected or Private block otherwise, and Public block always, with edge traversal disabled. This keeps selected home multiplayer available, refuses unsolicited public-network inbound traffic, and lets reinstall/uninstall remove both exact rules together. The elevated add/query/delete lifecycle and prompt-free real launch remain field gates.
- Final internal firewall-enabled setup `0.6.18` compiled at SHA-256 `43a0095e1d1bc2ab9c4ff80d69ad1546a17fd1556dad7f78d56d82a941542742` and passed Defender engine `1.1.26080.3`, intelligence `1.459.256.0`, with no new detection. Reinstall opt-out removes stale project rules before deciding whether to recreate them. It is not approved for handoff until the interactive renderer, gameplay, elevated firewall, and uninstall gates pass.
- Retained A/B evidence retired the `/gosnovideo` baseline. Vengeance's original intro visibly played with the MW3-derived wrapper plus `keepvidmem(1)`; Mercenaries' intro visibly played only with clean upstream DDrawCompat plus `noactivateapp(0)`. Cross-using those DLL/profiles caused surface-loss failures. These pairings prove the original MPEG assets and system codecs work, but they are no longer the packaging direction.
- Internal setup `0.6.16` packages that split presentation boundary at SHA-256 `1762572a25d6d5fb57c738a1a3bfa1e46085ec74279f0a9cf7b85335725ce437`. Release-tree policy passed and Defender engine `1.1.26080.3`, intelligence `1.459.256.0`, reported no threat or matching detection/remediation event. A clean seven-ISO staged worker exact-verified the complete Vengeance/Black Knight/packs and Mercenaries trees, then safely rolled both back because the user's existing installation correctly retained ownership of the per-user game registrations. All seven owned images were detached. The real setup upgrade plus menu/Alt-Tab/video acceptance remains the next field gate.
- Field candidate `0.6.7` completed elevated ISO/ZIP installation and all three titles reached their real menus, then pilot creation exposed the missing AutoConfig INI contract. The current source seeds the complete graphics/sound/special-command pages and launches at the games' supported 1024×768×32 surface. Because MW4 can rewrite an existing graphics page to 800×600×16 during startup, a bounded 30-second guard now repairs those three required keys in place while preserving controls and other user settings; focused tests cover both initial seeding and rewrite repair.
- The retired DDrawCompat package needed separate Vengeance/Mercenaries activation profiles and forced Black Knight to an 800×600 windowed fallback after `FEEDFACE` exits. Stock dgVoodoo's successful three-title load smoke removes that architecture exception; the source now stages the same exact-hash wrapper family beside Vengeance, `MW4X`, and Mercenaries and no longer forces Black Knight `-window`. Interactive qualification remains open.
- The former black/flickering-audio diagnosis was not a codec defect. The media is MPEG-1 video plus MP2 audio and the machine already has the required x86 DirectShow components. The actual failures were title-specific wrapper/activation interactions. Do not install or ship a system codec pack.
- Final documentation-correct local setup `0.6.15` is SHA-256 `680d1805b0392f199b23edd73a7363f998fab5a4f620198912080c75fb3d4919`. Its release tree passed policy; the runtime-identical `0.6.14` staged worker consumed all seven available ISO inputs and exact-verified both physical game trees. Defender intelligence `1.459.248.0` reported zero target detections for `0.6.15` with real-time protection enabled. It remains internal until pilot creation, one mission per title, unified uninstall, and interactive setup foreground behavior pass.
- The prior post-UAC `SetForegroundWindow`/`BringToFront` attempts were field-proven insufficient. `0.6.9` additionally makes the explicitly invoked Inno wizard `fsStayOnTop` for its lifetime. The exact setup is 171,551,426 bytes, SHA-256 `8e0a413c8084b85b5a1fbdeda3b616bbbd60ecd2bd3329a4f5875ab511cefe68`, and produced zero Defender target detections with real-time protection enabled and intelligence `1.459.248.0`. Pilot creation, fullscreen behavior, and topmost behavior remain field gates.

- Field setup `0.6.4` failed before mutation because its UI quietly required a separate Mercenaries fix ZIP; the retained log proved the five selected game/pack archives were valid and the missing PR1 source was the sole validation error. Internal candidate `0.6.6` now build-time extracts only exact-hash `Patchw32.dll` and `English/MW4MERCS.RTP` from the qualified official `mercpr1.exe`, packages them as an internal update payload, and automatically routes them before user media. A full worker run using Vengeance, Black Knight, both Mech Paks, and Mercenaries media with no user-supplied patch reached exact verification for both physical game trees; the sandbox then denied HKCU registration and the worker ownership-safely rolled both trees back. Public redistribution of the official update remains a legal/provenance gate.
- Internal `0.6.7` also explicitly foregrounds the Inno wizard during initialization and again once its first page is visible after UAC. Launcher uninstall now deduplicates the shared Vengeance/Black Knight physical root, attempts ownership-safe removal even from repair state, preserves unverifiable files, skips registry removal when no trusted launch path exists, and always hands application-shell removal to Inno. This fixes the field trap where manifestless saved logs/options prevented removal of the launcher. Exact setup SHA-256 is `c9a3ff437987f737d822382c78eec20b13595104d1820ab70216a47dfdf77e09`; current Defender reported zero target detections. Foreground behavior and the complete elevated ISO/ZIP run still require field confirmation.

- Candidate `0.5.7` is invalid and must not be handed out. It fails at startup with Inno Setup `Runtime error (at 36:461): Could not call proc.` because its license page is created from the Add Media handler instead of during wizard initialization.
- The user's field run also disproved the prior runtime gate: all three titles reached `MechWarrior 4 has been incorrectly installed`. A responsive process/window and zero-length game log can describe that error dialog, so the earlier seven-ISO runtime result is downgraded. No title is currently launch-qualified.
- `active/ECOSYSTEM_REVIEW.md` records the architecture reset. Established recipes consistently apply Vengeance Patch 3, Black Knight PR1, and Mercenaries PR1 before separate DRM/rendering work; keep Black Knight in Vengeance's `MW4X` subtree; and do not use one shared aggressive launch profile. Reference-install parity and dialog-aware menu/gameplay proof now precede another installer build.
- Black Knight PR1 starts from exact protected executable `be9c1573…`, uses setup-only capture DLL `7fbf1fbd…` to obtain the already decrypted user-media image in owned scratch, and now emits deterministic static v4 executable `b31bd051…` plus setup-accepted `EBUEula.dll` `d150fceb…`. The launcher starts `MW4x.exe` directly; no capture DLL/configuration/mapped image, injector, helper, driver, EULA interruption, or launch elevation remains in the installed tree. Unsupported retail input is rejected before commit.
- Internal setup `0.6.4` is 166,396,373 bytes with SHA-256 `9fa59dae22176427933f16fe59a991100a977a505e11253fb6a521cd64b647b1`. It rejects unsupported retail Black Knight before commit and contains the qualified EULA and consumed-registration fixes. Release-tree policy passed and the exact setup passed Defender engine `1.1.26080.3`, intelligence `1.459.239.0`; the rebuilt 389-file Vengeance/Inner Sphere/Black Knight tree and fresh Mercenaries PR1 tree passed the same current Defender baseline. The elevated packaged media flow, repair/unified shell uninstall, and actual menu/gameplay remain before user handoff. Internal `0.6.1` through `0.6.3` are superseded and must not be offered.
- The exact staged `0.6.4` application payload then ran its packaged self-contained worker with all three games in one transaction set. Vengeance/Inner Sphere/Black Knight and Mercenaries both exact-verified; launcher status reported all three games Ready, all three manuals present, Inner Sphere Ready, Clan Missing, and all three media icons present. The self-contained launcher remained responsive with title `MechWarrior 4 Remastered`; the complete application-plus-games tree passed Defender. Ownership-safe cleanup removed 390 and 212 owned paths plus matching registration values while retaining the staged launcher/manual shell. Native screenshot automation was unavailable, so visual pixel acceptance remains separate.
- The old compiled-package gate is obsolete. The next package gate is blocked intentionally until reference-install parity, official point-release state, and dialog-aware menu/gameplay proof pass.
- The active installer now models Vengeance and Black Knight as one atomic physical tree. Schema-2 ownership manifests record installed components; Black Knight is discovered from the Vengeance manifest at `MW4X\MW4X.exe`; destination planning emits one Vengeance-family transaction; and Black Knight-only media is blocked until a safe aggregate rebuild path exists. This structural slice passes the core and package/user-flow contracts but is not runtime qualification.
- Packaging source now declares one exact-hash setup-only Black Knight capture DLL plus its GPL license, corresponding source archive, and exact patch. The retired project capture host is absent. The managed static transform is embedded in the core; the x86 RTP host still accepts only exact hashes for Vengeance Patch 3, Black Knight PR1, and Mercenaries PR1.
- Corrected hosted runs passed: application contracts `35158374157` and compatibility source build `35158191892`. The hosted MSVC 17.14 build produced DLL `1e91f804…`, different from the runtime-qualified local MSVC 17.12 DLL `ff530c81…`; it is recorded as reproducible-source evidence but is not promoted without its own runtime and Defender qualification.
- This workspace has a narrow launcher, media-first setup, media recognition, manual pipeline, release-tree policy, and rollback-safe staging for all three games. Installation mechanics are substantially tested, but field evidence invalidated the runtime and packaged-setup qualifications; there is no public release or qualified gameplay baseline.
- Local inputs include two-disc Vengeance media, Black Knight media, two-disc Mercenaries media, Inner Sphere and Clan Mech Pak media, archival ZIP variants, patch/fix archives, and three raw PDF manuals. They are evidence/user inputs and are intentionally ignored.
- The intended product is one MW4-themed installer and launcher for Vengeance, Black Knight, and Mercenaries. Optional Inner Sphere and Clan packs are selected/detected at install time and surfaced as status in the launcher.
- Raw manuals now have a reproducible cleanup and verification pipeline. The three exact-hash cleaned PDFs and deterministic launcher cover PNGs are included in internal local packages but remain ignored and blocked from public release until redistribution rights are established.
- The desired app includes supported media detection, reproducible no-disc/compatibility transforms, per-game launch buttons/icons, pack status, manual links, repair/configuration, diagnostics, and a safe uninstaller that preserves user-owned saves/configuration by explicit policy.
- The user's freeware statement is not treated as sufficient redistribution authorization. Until reliable primary evidence says otherwise, releases require user-supplied media and exclude ISOs, serials, extracted game trees, and third-party cracks.
- Inner Sphere and Clan setup/DRM components are 32-bit, but their C-Dilla activation stack explicitly references `CDILLA16.EXE`. Disc/readme/archived-support evidence now separates plain resource payload, required Vengeance 3/Black Knight 1 patching, and SafeCast entitlement; the clean entitlement replacement still needs controlled before/after proof.
- Root `.env` is local-only. Its configured auxiliary model names are recorded without credentials in `active/AUXILIARY_MODELS.md`.
- The root Git repository is published publicly at `Icehellionx/MechWarrior-4-Remastered`; local `main` tracks `origin/main`.
- The .NET 10 WinForms launcher now uses the MW3 launcher's proven visual hierarchy: three game operations with their original per-title media icons, three manual operations with real generated manual-cover artwork directly below, compact Inner Sphere/Clan indicators, status at lower-left, and one uninstall action at lower-right. Only manifest-verified games launch, manuals enable only when present, and pack status derives from exact pack-file evidence in the verified Vengeance manifest.
- The launcher now embeds the user-provided original Vengeance icon treatment with only the exact silver/gray `R` glyph from the installed MW3 Remastered launcher composited at native 32×32 scale. The nearest-neighbor preview master and Windows icon live in `assets/branding/`; the underlying game-art redistribution status remains a release gate.
- A media catalog and directory inspector recognize both Vengeance discs, Black Knight, both Mercenaries discs, both Mech Paks, and the qualified Mercenaries PR1 payload. Unsafe paths fail closed; recognized media reports crack/DRM paths for mandatory exclusion.
- An ownership-aware ISO media session now validates regular `.iso` inputs, refuses pre-attached images, mounts explicitly read-only through a bounded Windows PowerShell adapter, validates the returned root, and dismounts only its owned image. ADR 0004 records the replaceable backend choice.
- Archival ZIP input now has an ISO-only extractor: it validates every entry, rejects unsafe/link/duplicate paths, enforces count and expanded-size limits, reports but never writes non-ISO files, verifies its exact extracted inventory, and commits atomically. ADR 0005 records why already-extracted trees and generic archive unpacking are excluded.
- `MediaSourceInspector` now composes directory inspection, owned ISO sessions, and ISO-only ZIP extraction behind one read-only call suitable for installer UI. The media probe is only a thin reporter over that service.
- `MediaSelectionSet` atomically converts recognized inspections into readiness for the three games and two optional packs. Its latest valid source wins per layout, failed mixed-media intake leaves prior state unchanged, and exclusion counts remain visible.
- The Inno package wizard is the sole visible installer. It requests elevation once, collects a neutral multi-file set of original ISOs or ISO-containing ZIPs before Install, requires at least one selection and explicit original-license acceptance, owns destination/progress/completion, and invokes `MW4RemasteredInstallWorker.exe` synchronously and hidden. The worker reopens and validates every selected source, plans complete games in dependency order, commits through the shared coordinator, verifies every result, writes all launch registration, and rolls back games committed earlier in the same failed run. The obsolete WinForms `InstallerForm` and its Start Menu shortcut are removed.
- Readiness cards now derive dependency warnings from `ProductDependencies` for every game and pack. Complete Black Knight or Mech Pak media without its Vengeance base is labeled `BASE MEDIA REQUIRED` rather than misleadingly appearing install-ready; no card contains a title-specific dependency special case.
- The installer previews a default per-user destination, allows folder selection, and reports conservative required/free space for complete game-media selections. Planning is UI-independent, emits games in catalog/dependency order, blocks Black Knight and Mech Paks unless Vengeance media is present or Vengeance is already installed, and returns explicit missing-dependency records. It rejects relative/filesystem-root/existing-file/reparse destinations and includes a 512 MiB reserve. The UI invokes the shared coordinator for all three games and passes selected pack sources into the Vengeance Patch 3/overlay path.
- Media inspection and revalidation now expose a Cancel action. Cancellation propagates through ZIP copying, ISO mount completion, source inspection, and selection reopen; existing ownership cleanup dismounts images and removes scratch before the UI reports cancellation.
- `MediaSourceSessionFactory` keeps directory/ISO/ZIP roots usable for an explicit lifetime and releases all owned mounts/scratch in reverse order. `MediaSelectionSessionFactory` reopens only current evidence, re-recognizes every expected layout, and closes earlier sources if a later source is missing or changed.
- A release-tree policy rejects disc images, secrets/keys, crack directories, legacy DRM files, reparse points, and executable/DLL/script content not named by an explicit allowlist.
- The manual pipeline reproducibly creates three ignored local outputs. Black Knight is 36 portrait pages with the front cover first and separated back cover last; Vengeance is 98 cover-derived 509.8×323-point pages; Mercenaries preserves its 19 original pages. Two consecutive runs produced identical hashes and every output page was rendered for review.
- The Vengeance install plan stages allowlisted files from both discs plus one exact-hash executable derived internally from the qualified retail loader/ICD/DPLAYER set. Public install requests contain only the two media roots; `VengeanceRetailExecutableTransform` produces the executable inside coordinator-owned scratch, and the coordinator removes that scratch on success or failure. The plan excludes `DPLAYERX.DLL` and all other setup/SafeDisc content, clears read-only media attributes, commits atomically, and writes a repair/uninstall ownership manifest.
- A real 1.04 GB disposable Vengeance tree exists under ignored `staging/vengeance-baseline`; independent manifest verification passes. It has not been launched.
- Black Knight's aggregate installer path requires qualified Mech Pak media, applies official PR1 in the shared tree, captures the exact decrypted PR1 image during setup, and installs only static v4 executable SHA-256 `b31bd051…`. A no-pack retail experiment crashed because the separate Vengeance Patch 2 dependency is not yet implemented, so setup rejects that combination explicitly.
- Mercenaries production intake requires the exact official PR1 updater from the supported fix ZIP. The archive boundary excludes its adjacent no-CD payload, the constrained RTP host applies all 11 qualified official changes in owned scratch, and a PR1-specific managed transform emits exact executable SHA-256 `7a806308…` while removing obsolete C-Dilla/ARTP imports and setup/media gates. A fresh worker smoke on `e054d34` consumed extracted copies of the two original ISOs plus the original fix ZIP, committed and exact-verified 211 owned files, cleaned all scratch, registered the title, and passed Defender. The exact launcher profile exposed one responsive `MechWarrior Mercenaries` window with no visible EULA, `STOP`, fullscreen, CD, or incorrect-install dialog. Ownership-safe removal deleted 212 owned paths and project-owned registration values while preserving the game-created log/settings and a synthetic save. The elevated packaged ISO/ZIP path, actual menu/gameplay, and reference-registry parity remain open.
- `GameInstallationCoordinator` provides one UI-independent orchestration path for all three games. It reports validation/transform/extraction/planning/commit/verification stages, owns Vengeance/Mercenaries transform and cabinet scratch cleanup on success or failure, delegates title allowlists to the existing builders, and returns success only after exact-tree verification. The development install probe consumes this service instead of duplicating orchestration.
- Pack evidence now shows the assets are 20 ordinary `RESOURCE` files, not 16-bit-encrypted data. The 32-bit setup/DRM stack invokes `CDILLA16.EXE`, on-disc documentation requires Vengeance 3 or Black Knight 1, and archived Microsoft support treats missing logos/content as a SafeCast licence problem. The replacement entitlement contract remains unqualified.
- `MechPakResourceOverlayPlanBuilder` captures the two exact 10-file resource allowlists, accepts only structurally recognized pack media, and targets only Vengeance or Black Knight. It excludes patches, setup, cracks, and DRM and is not connected to product install orchestration.
- `OwnedInstallOverlayTransaction` can safely merge such non-colliding payloads into a verified matching install: it stages additions, atomically replaces the manifest, rolls files back on manifest-commit failure, preserves unowned saves, and makes overlays removable by the existing uninstaller. It is not wired to production pack installation until patch/entitlement proof exists.
- The uninstaller core removes only manifest-owned files, preserves unowned saves/configuration, blocks before mutation on modified owned files, and rolls back move-phase failures. ADR 0003 records the policy.
- The single launcher uninstall action verifies every detected game first, blocks if any ownership state needs repair, removes verified manifest-owned game payloads while preserving unowned saves/configuration, then closes the launcher and starts the standard package uninstaller in visible-progress silent mode for the application shell, manuals, shortcuts, and registration. A field run of the older path ultimately removed the shell after its asynchronous delay; the new immediate launcher exit and visible progress address the misleading appearance that only games were removed.
- Manifest verification has explicit scopes: exact-tree verification rejects all unexpected files for staging/release gates, while owned-file verification powers launcher health so user-created saves/configuration do not disable a valid install. Both scopes still reject missing or modified owned game files.
- Vengeance, Black Knight, and Mercenaries consume the temporary `CDPath`, `EXE Path`, and `Version` registration values after first launch while retaining their own settings key. Setup writes the exact values initially; later launch validation accepts only either the complete exact tuple or the observed all-consumed state, and still rejects partial or conflicting ownership. No guessed `FIRSTRUN` value is written.
- The upstream elevated `VersionInjector.exe` and the retired project remote-thread capture host remain forbidden. The pinned GPL DLL is used only by the elevated install worker in owned scratch, with exact input/output hashes and deletion before commit; normal launch contains no SafeDiscLoader2 file. The capture bundle's corresponding source and exact patch are locked, but the revised package still needs a fresh Defender gate, full setup/repair/uninstall smoke, and interactive menu/gameplay qualification.
- The installer reopens selected evidence for the complete installation lifetime, streams coordinator stages to the batch UI, supports cancellation through chunked staging, and reports success only after exact-tree verification. The obsolete hidden Black Knight-only path remains removed: Vengeance is the required base, and a ready existing Vengeance install may satisfy that dependency for a later Black Knight add-on run.
- Vengeance and Mercenaries are SafeDisc 1.50.20 loader/ICD pairs, outside SafeDiscLoader2's documented 2.0–4.9 range. Both now use independently reviewable, exact-input, source-owned ICD transforms. Historical unSafeDisc tooling was never executed natively and is not a product or release dependency.
- Official Vengeance Patch 2 was successfully applied to a media-derived tree by calling the original 32-bit `Patchw32.dll` engine from a disposable explicitly `asInvoker` wrapper; the legacy `PatchApply.exe` itself is not required. The result retained the 344,863-byte SafeDisc loader and produced a 3,616,885-byte `MW4.ICD`, confirming that the official update does not remove the raw-disc check. SafeDiscShim handled the removed-driver calls, but both a virtual-label experiment and the mounted ISO still failed the sector check because ordinary ISO images do not preserve SafeDisc's physical signature.
- Static analysis and independent archival version tables identify Vengeance's 138,752-byte `DPLAYERX.DLL` as SafeDisc 1.50.20/r4. The earlier `r0/r1` wording was incorrect: those labels describe relocation handling inside the historical research tool, not the player generation. The ICD uses a 32-round TEA-family block cipher plus a DPLAYER-derived page-local second layer.
- The block cipher and complete 1.50.20 second-layer modifier chain are independently implemented in managed code and locked with non-game vectors. An ignored Unicorn harness emulated the official player initialization and selected historical research routines without native DLL loading, driver access, or starting the unsigned tool as a Windows process. Correctly supplying the SafeDisc loader, rather than DPLAYER itself, exposed the embedded key metadata; export-name validation then isolated the retail Vengeance relocation word with 192 of 194 imports matching versus 44 for the next candidate.
- The managed product transform now unwraps the original retail `01.06.11.0220` ICD, repairs imports and protected pointers, canonicalizes legacy loader padding, normalizes the PE end, and changes one uniquely matched disc-check branch. Its deterministic 3,497,984-byte result has SHA-256 `48ba4baf0894f0c14c44ddaf503cf953f59be3b919817156f1f11ea9b0df0de4` and opened a responsive `MechWarrior Vengeance` window without mounted media or UAC. The earlier emulated oracle differs only in eleven inert padding words left by the historical loader. No historical executable or emulator ships.
- A fresh end-to-end Vengeance coordinator run from the two original ISOs committed 230 payload files, excluded `MW4.ICD`, `DPLAYERX.DLL`, `SECDRV.SYS`, and setup, passed exact-tree verification and a bounded Defender scan, opened a responsive game window without media or UAC, and removed 231 owned files while preserving a synthetic unowned save. The next Vengeance gate is the official patch chain and broader runtime testing.
- `third_party/VengeancePatch2.lock.json` records the exact local archive/components and all 13 changed-output hashes as a regression oracle. It explicitly does not authorize redistribution or make the proprietary patch engine a product dependency.
- Windows installer detection was independently reproduced: unmanifested legacy tools with setup-like names requested elevation, while an otherwise identical disposable copy with an embedded `asInvoker` manifest ran normally. Project executables retain explicit `asInvoker` manifests; only the outer Inno setup requests the one deliberate administrator boundary for ISO access.
- Defender traced the visible warning to `Program:Script/Wacapew.A!ml` in an ignored historical SafeDiscLoader 1 `SDLoader.dll` and a disposable executable embedding it. Both were quarantined and were never product inputs. Exact opaque/generated research binaries were removed, known injector/unwrapper/patch/dump names are now hard-forbidden by release policy, and ADR 0008 requires current-engine scans plus detection-event review with no exclusions or warning suppression.
- ADR 0006 fixes the privilege contract: normal game launch never elevates; the upstream `VersionInjector.exe` and legacy unwrappers are explicitly forbidden from release trees even if someone adds them to an executable allowlist. ADR 0007 fixes the reproducible media-derived Black Knight payload contract.
- ADR 0009 fixes the per-user package boundary. A pinned Inno Setup 7.1.0 compiler produces a deterministic setup with no broad uninstall-delete behavior; signing and SmartScreen reputation remain separate release gates.
- Application CI now rechecks the core smoke suite, installer/launcher builds, privilege manifests, release-tree policy, packaging contract, and compatibility-source contract on a hosted Windows runner without uploading binaries. Defender/package qualification remains a separate local release gate.
- The latest user review rejected both the shell-first flow and the package launching a second installer. Setup now collects media before Install and remains the only visible wizard; its contained worker performs the qualified dependency-ordered installation without another window.
- Field testing exposed launch defects hidden by earlier short responsive-window smokes. The direct 32-bit HKCU records and shared aggressive launch profile are hypotheses, not locked contracts. The next baseline must capture original-installer HKCU/HKLM state and use minimal per-title arguments; Black Knight's distinct `-noautoconfigx` behavior is part of that check.
- The manual cover pipeline renders exact-hash PNGs from the cleaned PDFs. A native render of the packaged launcher confirms all three manual tiles show artwork rather than the generic letter fallback. Mercenaries has no separate cover page in the supplied PDF, so its designed opening spread is used.
- Internal setup `0.5.2`, built from source commit `ec40669`, is 169,840,365 bytes with SHA-256 `6ee1bd2411cab95617d9a1f22ee69338e8e27c7e47dd25e596e4ca36c3e709ec`. It has one visible media-first wizard and a synchronous hidden worker, includes the source-built Patch 3 host, three cleaned manuals, per-game launcher icon support, corrected Black Knight and Mercenaries installed names, and title-local `DSETUP.DLL`, but no proprietary patch engine/payload. A packaged two-disc Mercenaries smoke installed successfully without a second window; unified uninstall removed the game and application shell while preserving unowned configuration by policy. The final setup passed Defender with antivirus/real-time protection enabled and intelligence `1.459.226.0`, with no recent detection reported. Interactive package clicking remains an acceptance test rather than an inferred pass.
- Official Vengeance Patch 3 from Inner Sphere media applies directly to original retail Vengeance; Patch 2 is not a prerequisite. Its exact RTP/engine inputs and changed outputs are recorded in `third_party/VengeancePatch3.lock.json`. Setup may load the exact engine supplied by recognized user media inside contained scratch, but never distributes or installs it.
- `VengeancePatch3ExecutableTransform` validates the exact patched loader/ICD/DPLAYER inputs and deterministically emits SHA-256 `9ad88d307bf666c76ffd953aba3ebff6069a87e3c36b81d674f5668acb4dd7f3`, removing the C-Dilla/ARTP imports and replacing all four deferred call sites with call-convention-correct local results. Transform v1 failed to clean the stdcall arguments and caused the field incorrect-install stop; v2 explicitly restores the stack. The source-owned asInvoker x86 host applies only exact official user-media input in minimal scratch. A fresh packaged Vengeance plus both packs remained responsive for 35 seconds with an empty log and no disc/UAC/legacy client files.

## Best resume path

1. Package the current post-`0.6.29` source correction reproducibly and scan the exact setup plus extracted and installed trees. Do not offer `0.6.29` again.
2. Field-verify the four selected Inner Sphere and four selected Clan chassis in Instant Action, multiplayer setup, and MechLab for Vengeance, Black Knight, and Mercenaries; confirm campaign markets were not broadened.
3. Exercise the centered borderless desktop-sized dgVoodoo window in all three titles: Alt-Tab must leave the frame visible in normal z-order, release input, allow another window above it, and return without corruption, a lost cursor, a display-mode switch, or a visible dgVoodoo watermark.
4. Compare maximum-detail gameplay and 2D screens at the largest aspect-preserving render resolution for 4x MSAA, 16x AF, 32-bit color, correct 4:3 geometry, readable text, and no cropped UI; include a second monitor/GPU class when available.
5. Exercise repair and unified uninstall from the new transformed manifests, proving owned transformed archives are removed while saves/settings and unowned files remain.
6. Continue the Vengeance-only intro zoom separately. A global `DisplayROI=16_9` is rejected because it would crop gameplay. Do not repeat the incompatible DDrawCompat swap or the unobserved stock-2.86.5 add-on probe; require a documented, actually loadable movie-only presentation boundary without `/gosnovideo` before integration.

## Known constraints and risks

- Licensing/freeware history may differ between Mercenaries releases and the original retail titles.
- Multiple archival ZIP/ISO layouts may contain different revisions or modified executables; filenames alone are not trusted.
- Opaque no-CD archives are high risk. Prefer known-input binary transforms or openly licensed source-based compatibility when legally and technically viable.
- Legacy setup/bootstrap executables may be 16-bit even when game payloads are 32-bit; direct payload extraction may be safer than emulating installers, but must preserve registry/configuration prerequisites.
- Current NVIDIA/Windows hardware is qualified through playable entry on `0.6.22`; every later presentation/Alt-Tab/pack change, other GPUs/monitors/input devices, and multiplayer remain unqualified.

## Last verification

The list below is a historical evidence ledger. Any entry that says a game opened or remained responsive is process-level evidence only and is superseded by the field-confirmed incorrect-install dialogs unless it explicitly identifies a real menu/gameplay path.

- Governance intake completed; local inputs enumerated without opening or publishing content.
- Auxiliary router syntax and four unit tests passed. Ollama was reachable with all nine configured local model names installed; Featherless roles are configured but were not live-billed.
- The launcher/core Release build completed with zero warnings/errors, and the synthetic core smoke test passed.
- The installer/core Release build completed with zero warnings/errors. Synthetic selection tests cover incomplete/complete two-disc readiness, multi-ISO ZIPs, optional packs, exclusion accounting, latest-source replacement, and atomic rejection of mixed unknown media.
- Synthetic destination-planning tests cover complete-game inclusion, contained title folders, safety reserve, sufficient/insufficient capacity, and rejection of relative, filesystem-root, or existing-file destinations. The revised installer and launcher were captured from real native windows; both had readable unclipped layouts at their fixed default sizes.
- Synthetic source-session tests prove ISO/ZIP resources live until disposal, normal and partial-failure cleanup, selection-wide reopen, changed-media rejection, and cleanup when a later source disappears.
- Cancellation regressions prove pre-extraction cancellation performs no mutation and cancellation arriving during mount dismounts the newly owned image before propagating.
- The refactored real Black Knight archival-ZIP smoke recognized the expected layout and two prohibited SafeDisc paths, returned exit 0, left no new media-session scratch, and confirmed the standalone local image was detached.
- Launcher process-boundary tests confirm that an unmanifested executable cannot be launched and that a verified game starts with its own directory as the working directory. A packaged installed-state smoke showed the window in 228 ms, kept it responsive during background verification, and settled with preserved Black Knight enabled as `LAUNCH GAME`.
- A local-only SHA-256 inventory recorded all 23 media/manual inputs under ignored `.local/`; no source media was changed.
- ISO directory inspection confirmed C-Dilla/SafeCast and SafeDisc-era files on both pack discs plus directly accessible content/patch payloads. The direct-extraction hypothesis remains unqualified.
- Read-only pack inventory confirmed 10 plain resource files (34,434,947 bytes) for Inner Sphere and 10 (20,285,495 bytes) for Clan. No Vengeance/Black Knight path collisions exist; four of six Mercenaries name collisions differ by hash, so a generic overlay is rejected.
- Synthetic pack-overlay tests prove exact 10-file selection, crack/DRM/RTP exclusion, Vengeance/Black Knight targeting, Mercenaries refusal, and partial-payload rejection.
- Synthetic overlay-transaction tests prove target/product validation, collision refusal, save preservation, modified-base blocking, rollback after a locked-manifest commit failure, final manifest validity, and overlay-aware uninstall.
- The real-media recognition smoke passed all seven supplied ISOs and confirmed every owned mount was detached afterward.
- The new C# owned-ISO boundary independently mounted and recognized the Black Knight image, reported both prohibited SafeDisc paths, and left the image detached. Synthetic lifetime tests cover pre-attached refusal and failed-root cleanup.
- The composed ZIP media probe extracted only `MW4BK.iso` from the real Black Knight archive, recognized the expected layout, and left no new scratch directory. Synthetic tests prove non-ISO exclusion and traversal rejection.
- Synthetic transaction tests cover containment, duplicate destinations, writable normalization, rollback, ownership manifests, tamper detection, and unexpected-file detection.
- Real Vengeance staging committed 231 files from two read-only mounted discs and the exact version-2.0 replacement input. Manifest verification passed, no staged files remained read-only, and both owned images were detached.
- The earlier unpatched Black Knight CI evidence (`f2671084…`) is superseded. The current patched build is `ff530c81…`, with patch `286de586…` and no helper; two clean local source builds were byte-identical.
- The exact current five-file Black Knight bundle was assembled locally; all hashes matched the lock and current Defender scans found no threats.
- Fresh corrected Black Knight staging committed and exactly verified 143 manifest-owned files from the recognized disc tree plus internal bundle. Setup-table long filenames and `DSETUP.DLL` are present; no replacement executable, mounted image, serial, or external helper was required.
- A disposable copy of that final tree passed uninstall: all 143 owned entries, including the manifest, were removed and the sole synthetic unowned configuration file was preserved.
- Real Mercenaries cabinet extraction committed and exactly re-inventoried 85 files. An earlier two-disc coordinator run committed 208 payload files plus its ownership manifest, produced transform-v1 executable SHA-256 `f92bbb8c…`, left no SafeDisc/C-Dilla/setup/crack payload, detached both images, passed exact verification and Defender, and opened a responsive game window without disc or UAC; field testing later proved that build still depended on an unavailable machine-wide setup record, which transform v2 corrects.
- A fresh corrected Mercenaries coordinator install committed 209 payload files plus its manifest, including `DSETUP.DLL`, and opened a responsive `MechWarrior Mercenaries` window. The stale installed-tree DirectPlay failure did not recur, and a separate 32-bit COM smoke successfully instantiated the DirectPlay object. Its earlier owned-file uninstall removed all owned files including the manifest and preserved the synthetic unowned save. Synthetic coordinator smoke observes completion for all three games and proves Mercenaries transform/cabinet scratch cleanup after success and injected planning failure.
- Coordinator and transaction cancellation tests prove a pre-commit cancellation leaves no destination. A rebuilt installer with the exact Black Knight bundle initialized responsively, and Defender found no threats in the combined output tree.
- A real-tree uninstall smoke removed all 232 owned payload/manifest files from a disposable 1.04 GB copy while preserving an unowned synthetic pilot save; the exact disposable target was then removed.
- Release-tree policy tests passed their safe baseline and rejected synthetic ISO, unallowlisted executable, SafeDisc driver, and crack-directory fixtures.
- All three manual outputs passed page-count/geometry/render checks and deterministic SHA-256 comparison. Final full contact-sheet review found no clipped or misordered pages.
- The former project-owned `win-x86` compatibility helper, its publisher, and its boundary test were removed after proving normal app-local `VERSION.dll` loading. It is not a fallback path.
- Fresh Release build trees for the installer, launcher, and Patch 3 host passed their privilege contracts; the exact 0.5.7 stage/setup/install tree passed the newer Defender gate recorded above.
- The fresh media-only Black Knight install launches directly with no mounted media or launch elevation. The bounded repeat test stopped only exact-path game processes, observed one process each time, and current Defender scanning reported no threats in the full install tree.
- GitHub CLI device authorization succeeded for `Icehellionx`; the public repository was created and the initial `main` branch pushed.
- Two clean final-flow package builds produced byte-identical 72,289,562-byte setup executables and identical checksum sidecars; setup SHA-256 is `1b7455ed7180f638a6e7c24b8d118a8ed4954f693a6bb7720658f8c544aa1994`.
- The final shell was installed to the intended per-user registration with its launcher hash matched to a fresh publish and the existing Black Knight directory preserved. An accidental empty test-registration path was removed through its own standard uninstaller before the correct registration was restored.
- A historical disposable per-user package/uninstall smoke preserved an unowned synthetic `black-knight/Saves/pilot.sav`. Current 0.5.7 deliberately moves UAC to setup because ISO access failed without it; the installed launcher and games remain non-elevated.
- The final reproducible setup passed the ADR 0008 Defender gate with engine `1.1.26080.3` and intelligence `1.459.223.0`, with no threat and no matching detection/remediation event.
- The corrected installed per-user shell plus preserved Black Knight tree passed the same current Defender gate with no threat or new detection/remediation event.
- Hosted Windows application-contract run `35055297275` passed all source smoke, build, privilege, release-tree, packaging, and compatibility-source checks for commit `9888e9a`; the workflow uploaded no binaries.
- Hosted Windows application-contract run `35061879507` passed the redesigned flow, including the new user-flow contract, for commit `11d0310`; the workflow uploaded no binaries.
- The clarified-scope package rebuilt successfully at 72,297,599 bytes with SHA-256 `a2b00bacf81b4ea1533ef8bb24170e48f04b5650942eb436bfdd05830a1e307d`. It passed release-tree policy and a current Defender scan (engine `1.1.26080.3`, intelligence `1.459.223.0`) with no detection or remediation event, then updated the intended per-user application shell without elevation.
- The unified `0.5.2` package built from `ec40669` at 169,840,365 bytes with SHA-256 `6ee1bd2411cab95617d9a1f22ee69338e8e27c7e47dd25e596e4ca36c3e709ec`. Its Inno wizard is the sole visible installer; the application worker is hidden and synchronous. A packaged two-disc Mercenaries end-to-end smoke verified the corrected setup-table names, all three manuals, obsolete-shortcut cleanup, and full game-plus-shell uninstall. The final setup passed a Defender custom scan with antivirus and real-time protection enabled, intelligence `1.459.226.0`, and zero recent detections.
- A precommit `0.5.3` setup installed fresh Vengeance and Mercenaries trees from the four original ISOs. Both were launched through the real product orchestration, retained their expected window titles for 35 seconds, produced no delayed STOP/exception/incorrect-install log, and required neither mounted media nor elevation. A native packaged-launcher render confirmed the three real manual-cover images and both installed game icons.
- Internal setup `0.5.3`, built from source commit `a549960`, is 169,873,594 bytes with SHA-256 `5770d4a1f1eb8f7b85f0159e5158d432e529fc90dd7f61eb88d1e7f68b59b6a0`. The exact setup passed release-tree policy and Defender engine `1.1.26080.3` with intelligence `1.459.226.0`, with no new detection or remediation event. It remains a local/internal artifact because manual and game-art redistribution rights are still unresolved.
- Interactive `0.5.3` setup exposed a reinstall defect: the prior safe uninstall correctly preserved `Mechwarrior4.txt` and Mercenaries `options.ini`, but the worker rejected their now-manifestless product directories before reinstall. The transaction now stages a fresh owned tree around non-colliding preserved files and atomically swaps it into place; owned-path collisions and existing manifests still fail without mutation. Worker logs now persist under the application `Logs` directory instead of the self-deleting Inno temp folder.
- A real four-ISO worker smoke seeded disposable Vengeance/Mercenaries destinations with the same log/settings residue found after the user's safe uninstall. Both complete games installed and verified successfully, the residue remained byte-for-byte, and the durable worker log recorded the full successful run.
- A fresh PR1 production-worker smoke consumed the original Mercenaries two-ISO ZIP and supported fix ZIP through the same elevated media boundary as Setup. It applied PR1, produced managed executable SHA-256 `7a806308…`, committed a 211-file exact ownership manifest, left no media-session scratch, removed its owned registration/files through project APIs, and preserved a synthetic unowned save. This is install/uninstall evidence only; no menu/gameplay claim was made.
- Internal setup `0.5.4`, built from source commit `cff40eb`, is 169,872,042 bytes with SHA-256 `5b422ee3a92ca879cf9fe339427036d12517624143f6aa3b622c19591f0e7021`. Release-tree policy passed, and the exact setup passed Defender engine `1.1.26080.3` with intelligence `1.459.226.0` without a detection or remediation event.
- A seven-media precommit `0.5.5` install provided the registry and renderer diagnosis. It is superseded by the 0.5.7 setup-owned registration and direct app-local Black Knight path. DDrawCompat v0.7.1 remains rejected; the qualified profile uses windowed OpenGL and no stacked wrapper.
- Final internal setup `0.5.5` is 169,890,506 bytes with SHA-256 `d9d999d007c67d2d4884a6beb1ebe252caba727da53b615977e0d6caf616d55c`. A fresh exact-package seven-ISO install reported all three games and both packs Ready. Vengeance and Mercenaries each remained alive for 40 seconds with empty logs and no incorrect-install stop; on this unattended desktop Windows reported their legacy windows nonresponsive, so interactive renderer acceptance remains open. Defender scanned both setup and the full installed tree with engine `1.1.26080.3`, intelligence `1.459.239.0`, real-time protection enabled, and no detection. Unified uninstall removed all manifest-owned game payloads and the complete application shell; only deliberately preserved game-created logs/configuration remained.
- No permanent product install, signing/SmartScreen qualification, or successful field/runtime qualification exists. The user's interactive package and game tests found fatal setup and incorrect-install failures; gameplay and repair remain unimplemented/unqualified.
